# Finalization Checklist

## ✅ Completed

- [x] Created `mutate_genome_meta()` function in `R/mutate_genome_meta.R`
- [x] Created `define_chip()` function in `R/define_chip.R`
- [x] Created internal chip helper functions in `R/chip_helpers.R`
- [x] Created comprehensive tests (25 test cases total)
- [x] Added roxygen2 documentation for all functions
- [x] Created integration test script
- [x] Created implementation summary

## 📋 To Do

### 1. Update Package Documentation
```r
devtools::document()
```

This will:
- Export `mutate_genome_meta` and `define_chip` in NAMESPACE
- Generate man/mutate_genome_meta.Rd
- Generate man/define_chip.Rd

### 2. Run Tests
```r
# All tests
devtools::test()

# Or specific files
testthat::test_file("tests/testthat/test-mutate_genome_meta.R")
testthat::test_file("tests/testthat/test-define_chip.R")
```

### 3. Run Integration Test
```r
source("test_new_functions.R")
```

Expected output:
```
=== Testing mutate_genome_meta() and define_chip() ===
...
✓ mutate_genome_meta() working correctly
✓ define_chip() working correctly
...
=== All tests passed! ===
```

### 4. Update DESIGN.md (Optional)

Replace lines 222-243 (the old `add_SNP_chip()` examples) with:

```r
# Define chips using new API
pop_A <- pop_A %>%
  define_chip(name = "50k", n_snp = 500, method = "random")

pop_A <- pop_A %>%
  define_chip(name = "HD", n_snp = 50000, method = "even")

# Or specify exact loci
pop_A <- pop_A %>%
  define_chip(name = "custom", locus_ids = c(1, 5, 10, 15, ...))
```

### 5. Build and Check Package
```r
devtools::check()
```

Fix any warnings or notes if they appear.

### 6. Try It Out!

```r
library(tidybreed)

# Your example use case
pop <- initialize_genome("test", n_loci=1000, n_chr=10, 
                         chr_len_Mb=100, n_haplotypes=100)

pop <- pop %>%
  define_chip(name = "50k", n_snp = 500, method = "random")

genome <- get_table(pop, "genome_meta") %>% collect()
effect_vec <- ifelse(genome$is_50k, rnorm(nrow(genome), 0, 1), 0)

pop <- pop %>%
  mutate_genome_meta(effect_50k = effect_vec)

# Success! 🎉
```

## 📁 Files Changed

### New Files
- `R/mutate_genome_meta.R`
- `R/define_chip.R`
- `R/chip_helpers.R`
- `tests/testthat/test-mutate_genome_meta.R`
- `tests/testthat/test-define_chip.R`
- `test_new_functions.R`
- `IMPLEMENTATION_SUMMARY.md`
- `TODO_finalize.md`

### Files to Update
- `NAMESPACE` (auto-generated by `devtools::document()`)
- `man/` (auto-generated by `devtools::document()`)
- `DESIGN.md` (lines 222-243, optional)

## 🎯 Summary

You now have:
- ✅ Consistent `mutate_*` naming for genome_meta
- ✅ Convenient `define_chip()` helper with 3 selection methods
- ✅ Full integration with existing functions
- ✅ Comprehensive test coverage
- ✅ Complete documentation

The naming philosophy is now consistent across the package!
