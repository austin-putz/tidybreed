# Regex that matches a strictly safe SQL identifier (letter, then letters/digits/underscores).

Regex that matches a strictly safe SQL identifier (letter, then
letters/digits/underscores).

## Usage

``` r
SQL_IDENTIFIER_RE
```
