# System-managed tables that users must not overwrite with `define_table()`

System-managed tables that users must not overwrite with
[`define_table()`](https://austin-putz.github.io/tidybreed/reference/define_table.md)

## Usage

``` r
SYSTEM_TABLES
```
