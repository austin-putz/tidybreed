# Resolve the archive DB file path from function arg + global options

Resolve the archive DB file path from function arg + global options

## Usage

``` r
.resolve_archive_path(pop, archive_path)
```
