# Print method for tidybreed_schema

Print method for tidybreed_schema

## Usage

``` r
# S3 method for class 'tidybreed_schema'
print(x, ...)
```

## Arguments

- x:

  A `tidybreed_schema` object from
  [`schema()`](https://austin-putz.github.io/tidybreed/reference/schema.md).

- ...:

  Additional arguments (not used).

## Value

`x`, invisibly.
