# Encode a named numeric vector as a small JSON string

Encode a named numeric vector as a small JSON string

## Usage

``` r
encode_levels_json(x)
```

## Arguments

- x:

  Named numeric vector.

## Value

A single character string, or `NA_character_` if `x` is empty.
