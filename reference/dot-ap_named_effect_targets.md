# The model-path phenotypes carrying each random effect, by effect name

The model-path phenotypes carrying each random effect, by effect name

## Usage

``` r
.ap_named_effect_targets(entries)
```

## Value

A list named by `effect_name` (byte-sorted) of the phenotypes whose
planned records touch that effect, in plan order.
