# Write user-supplied phenotype values verbatim

Write user-supplied phenotype values verbatim

## Usage

``` r
write_user_phenotype_values(
  pop,
  phenos,
  subset_by_pheno,
  user_values,
  extra_cols = list()
)
```
