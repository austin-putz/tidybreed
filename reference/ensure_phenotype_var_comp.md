# Ensure the phenotype_var_comp table exists in the database

Ensure the phenotype_var_comp table exists in the database

## Usage

``` r
ensure_phenotype_var_comp(pop)
```

## Arguments

- pop:

  A `tidybreed_pop` object.

## Value

The `tidybreed_pop` with `$tables` updated.
