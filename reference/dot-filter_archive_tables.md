# Filter a table list to those present in the working DB; message about the rest

Filter a table list to those present in the working DB; message about
the rest

## Usage

``` r
.filter_archive_tables(tbls, db_tables, category)
```
