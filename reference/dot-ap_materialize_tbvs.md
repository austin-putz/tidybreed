# Materialize the TBVs Stage 1 reads (simple, composite, formula_tbv)

Materialize the TBVs Stage 1 reads (simple, composite, formula_tbv)

## Usage

``` r
.ap_materialize_tbvs(
  pop,
  tbl,
  phenos,
  has_components,
  has_formula_tbv,
  has_formula,
  components_by_pheno,
  formula_tbv_str,
  subset_by_pheno
)
```
