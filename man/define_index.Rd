% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_index.R
\name{define_index}
\alias{define_index}
\title{Define a selection index}
\usage{
define_index(
  pop,
  index_name,
  trait_names,
  index_wts,
  economic_wts = NULL,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{index_name}{Character scalar. Name of the index (e.g. \code{"terminal"},
\code{"maternal"}). Must be a valid SQL identifier.}

\item{trait_names}{Character vector. Trait names to include in the index.
Each must be a valid SQL identifier. Order must match \code{index_wts}.}

\item{index_wts}{Numeric vector. Selection index weights, one per trait in
\code{trait_names}. Positive weights favour higher trait values; negative
weights favour lower values.}

\item{economic_wts}{Numeric vector (optional). Economic value per unit for
each trait, in the same order as \code{trait_names}. Some values may be \code{0}.
When \code{NULL} (default), the \code{economic_weight} column is not written.}

\item{overwrite}{Logical. If \code{FALSE} (default), re-calling for an existing
\verb{(index_name, trait_name)} pair is a no-op — existing weights and economic
values are preserved. If \code{TRUE}, all non-key columns (\code{index_weight},
\code{economic_weight}, and any \code{...} columns) are updated in place.}

\item{...}{Optional extra columns to add to \code{index_meta}. Scalars are
broadcast to all rows (one per trait). Vectors must have length equal to
\code{length(trait_names)}. Types are inferred via \code{\link[=infer_duckdb_type]{infer_duckdb_type()}}.}
}
\value{
The \code{tidybreed_pop} (invisibly). Assign the result back.
}
\description{
Registers a named selection index in the \code{index_meta} table by specifying
which traits are included and their weighting coefficients. Call this once
per index (before calling \code{\link[=add_index]{add_index()}}). Re-calling with the same
\code{index_name} and \code{trait_name} pairs updates the weights in place.

Not all traits in the population need to appear in an index — you may have
five traits but index only three. Traits not listed in \code{trait_names} are
ignored when \code{\link[=add_index]{add_index()}} computes the index value.
}
\examples{
\dontrun{
# Index weights alone
pop <- define_index(pop, "terminal",
                    trait_names = c("ADG", "FCR"),
                    index_wts   = c(1.2, -0.8))

# Also record the underlying economic weights (per-unit $ value), stored
# alongside index_weight in index_meta$economic_weight — e.g. for later use
# with add_tbv(index_names = ..., type = "economic")
pop <- define_index(pop, "maternal",
                    trait_names  = c("ADG", "FCR"),
                    index_wts    = c(0.5, -0.3),
                    economic_wts = c(2.5, -1.0))

# With an extra metadata column
pop <- define_index(pop, "growth_only",
                    trait_names = c("ADG"),
                    index_wts   = c(1.0),
                    source = "genetic_team_v2")

# Re-defining an index: overwrite = FALSE (default) is a no-op if the
# (index_name, trait_name) pair already exists — the call below does NOT
# change "terminal"'s weights since overwrite defaults to FALSE
pop <- define_index(pop, "terminal",
                    trait_names = c("ADG", "FCR"),
                    index_wts   = c(1.5, -0.5))

# overwrite = TRUE updates the weights in place
pop <- define_index(pop, "terminal",
                    trait_names = c("ADG", "FCR"),
                    index_wts   = c(1.5, -0.5),
                    overwrite   = TRUE)
}
}
\seealso{
\code{\link[=add_index]{add_index()}}, \code{\link[=define_trait]{define_trait()}}
}
