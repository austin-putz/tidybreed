% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haplotype_write_helpers.R
\name{detect_available_memory}
\alias{detect_available_memory}
\title{Detect available system memory in bytes (cross-OS, best-effort)}
\usage{
detect_available_memory()
}
\value{
Numeric scalar bytes, or \code{NA_real_}.
}
\description{
Returns available (not total) physical RAM in bytes on Linux, macOS
(Intel + Apple Silicon), and Windows, or \code{NA_real_} when detection fails or
the OS is unsupported. \strong{Never errors} — callers fall back to a conservative
fixed budget when this returns \code{NA}.
}
\details{
\itemize{
\item Linux: \verb{/proc/meminfo} \code{MemAvailable} (falls back to \code{MemFree}), in kB.
\item macOS: \code{vm_stat} free + inactive + speculative pages x \code{hw.pagesize}.
\item Windows: \verb{wmic OS get FreePhysicalMemory} (kB), then a PowerShell fallback.
}
}
\keyword{internal}
