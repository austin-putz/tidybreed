% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_phenotype_stages.R
\name{add_phenotype_stages}
\alias{add_phenotype_stages}
\title{The three stages of \code{add_phenotype()}}
\description{
\code{add_phenotype()} runs in three stages with a strict boundary between them:
\itemize{
\item \strong{Stage 1 — PLAN} (\code{.ap_plan()}): decide the final record list for every
phenotype. Sex expression, the repeatable guard, fixed-effect
contributions and \code{null_class_action = "skip"}, formula/composite TBV
evaluation and \code{missing_component_action}, path classification,
\code{pheno_number} assignment, the residual condition value of every record,
and the random-effect level every record touches. \strong{No random number is
drawn and nothing is written} (the one prerequisite write is
\code{\link[=add_tbv]{add_tbv()}}, which materializes the TBVs the plan reads and is
RNG-neutral).
\item \strong{Stage 2 — RESOLVE} (\code{.ap_resolve()}): every random draw of the call,
in a fixed order, and the liability / type conversion — all in memory.
Nothing is written. Two adapters over \code{\link[=find_covariance_blocks]{find_covariance_blocks()}} and
\code{\link[=resolve_correlated_draws]{resolve_correlated_draws()}} draw everything: the named-effect adapter
(\code{.ap_resolve_named_effects()}, entity \verb{(effect_name, level)}, stored
in \code{phenotype_random_effects}, one draw per level reused forever), then
the residual adapter (\code{.ap_resolve_residuals()}, entity
\verb{(id_ind, pheno_number)}, stored in \code{ind_phenotype}, one draw per
record). In both, an entity draws its planned coordinates from the
block's Gaussian conditional on the coordinates it has already
realized — stored on disk from an earlier call, or (residuals only)
fixed by \code{user_residual}.
\item \strong{Stage 3 — COMMIT} (\code{.ap_commit()}): one transaction that inserts the
new \code{phenotype_random_effects} rows and the \code{ind_phenotype} records via
\code{duckdb_register()} + \code{INSERT}. No random number is drawn, so the stream
a call consumes is a function of the model and the plan only.
}

The consequence is that a record that will not exist — an individual
skipped by \code{null_class_action}, excluded by a missing component, or
refused by the repeatable guard — never consumes RNG and never leaves
stochastic state behind. Planned records are ordered by \code{id_ind} within a
phenotype; that order is what \code{user_values} and \code{user_residual} match
positionally, and it does not depend on physical row order in the
database.

See \code{plans/sample_correlated_effects.md} §5.5.
}
\keyword{internal}
