% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{.schema_size_label}
\alias{.schema_size_label}
\title{Whole-database size label for the \code{schema()} header}
\usage{
.schema_size_label(conn, db_path)
}
\arguments{
\item{conn}{A DBI connection.}

\item{db_path}{Character. \code{pop$db_path} — the literal \code{":memory:"} for an
in-memory population.}
}
\value{
A single string such as \code{"4.0 MiB on disk"}, or \code{NA_character_} if
the pragma is unavailable.
}
\description{
\verb{PRAGMA database_size} returns one row of already-formatted strings, so no
byte arithmetic is needed. Two cases:
}
\details{
\itemize{
\item \strong{File-backed}: report \code{database_size}, plus \code{wal_size} when the WAL is
non-empty. The WAL is only folded into the file by a \code{CHECKPOINT}, so a user
looking at the \code{.duckdb} on disk sees both; reporting only \code{database_size}
under-reports. \code{schema()} must not checkpoint — that is a write.
\item \strong{In-memory} (\code{db_name = ":memory:"}): \code{database_size} is \code{"0 bytes"} and
\code{block_size} is \code{0} because there is no file. Report \code{memory_usage} and
label it as memory, not disk.
}
}
\keyword{internal}
