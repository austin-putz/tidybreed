% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{define_schema_description}
\alias{define_schema_description}
\title{Define or update a description for a table or column}
\usage{
define_schema_description(tbl, column_name = NULL, description, notes = NULL)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{\link[=get_table]{get_table()}}. The table name is
inferred from \code{tbl$table_name}.}

\item{column_name}{Character or \code{NULL}. When \code{NULL} (default), the
description applies to the table itself. When a column name is given,
the description applies to that specific column.}

\item{description}{Character. Human-readable description.}

\item{notes}{Character or \code{NULL}. Optional supplementary context.}
}
\value{
The \code{tidybreed_table}, invisibly, enabling back-to-back chained
calls without repeating \code{get_table()}. The underlying database is modified
in place via the DBI connection — do not assign the result back to \code{pop}
(use as a side-effect or extract with \code{tbl$pop}). \code{schema()} and
\code{describe_table()} accept either \code{tidybreed_pop} or \code{tidybreed_table}.
}
\description{
Upserts a description into \verb{_schema_meta}. Use this to document custom
columns added via \code{mutate_table()} or \code{...} in \code{add_founders()} etc.
Pipe a \code{tidybreed_table} from \code{\link[=get_table]{get_table()}} as the first argument.
}
\examples{
\dontrun{
# Chain multiple column descriptions — no need to repeat get_table()
pop |>
  get_table("ind_meta") |>
  define_schema_description("id_ind",    "Unique individual identifier") |>
  define_schema_description("sex",       "Sex of individual (M or F)")   |>
  define_schema_description("line_name", "Genetic line name")
describe_table(pop, "ind_meta")  # pop still valid; DBI conn is a reference

# Table-level description (column_name = NULL)
pop |>
  get_table("ind_meta") |>
  define_schema_description(description = "Individual metadata table")
}
}
\seealso{
\code{\link[=describe_table]{describe_table()}}, \code{\link[=schema]{schema()}}
}
