% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ploidy_helpers.R
\name{assert_diploid_only}
\alias{assert_diploid_only}
\title{Assert that \code{chr_meta} has no non-diploid chromosomes}
\usage{
assert_diploid_only(pop)
}
\arguments{
\item{pop}{A \code{tidybreed_pop}.}
}
\value{
Invisible \code{NULL} on success; errors otherwise.
}
\description{
Ploidy-aware dosage and genotype extraction is Stage 4 (\code{define_chr()})
scope. Until that ships, any \code{chr_meta} row with \code{copies_M}/\code{copies_F} other
than 2 would silently produce wrong dosage sums rather than erroring, since
the current dosage/export SQL assumes exactly two haplotype rows per
individual per locus. \code{define_chr()} does not exist yet, so this guard is
forward defense — it protects the day \code{chr_meta} gains non-default rows,
not a fix for a currently reachable bug.
}
\keyword{internal}
