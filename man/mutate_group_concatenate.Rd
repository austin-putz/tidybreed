% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_group_concatenate.R
\name{mutate_group_concatenate}
\alias{mutate_group_concatenate}
\title{Create a composite group column by concatenating existing columns}
\usage{
mutate_group_concatenate(
  tbl,
  col_name,
  source_columns,
  sep = "_",
  null_action = "propagate",
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{get_table()} (optionally filtered).}

\item{col_name}{Character scalar. Name of the group column to create or
update. Must be a valid SQL identifier and not a reserved column.}

\item{source_columns}{Character vector of one or more column names in the
same table to concatenate. Order is preserved.}

\item{sep}{Character scalar. Separator string. Default \code{"_"}.}

\item{null_action}{One of \code{"propagate"} (default), \code{"skip"}, or
\code{"literal"}. See section NULL handling above.}

\item{overwrite}{Logical. If \code{FALSE} (default), error when any filtered row
already has a non-\code{NULL} value in \code{col_name}.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages.}
}
\value{
The modified \code{tidybreed_pop} object (invisibly).
}
\description{
Deterministically builds a \code{VARCHAR} group label for each filtered row by
concatenating the values of one or more existing columns in the same table,
separated by \code{sep}. Useful for contemporary groups (\code{sex_line_gen}), sib
groups (\code{sire_id_dam_id}), and any label derived from existing row data.

Pipe \code{get_table("ind_meta")} — optionally through \code{dplyr::filter()} — into
this function. All \code{source_columns} must exist in the same table as the
target column. No randomness is involved; results are fully deterministic.
}
\section{NULL handling}{

The \code{null_action} argument controls what happens when any source column
value is \code{NULL} (\code{NA}) for a given row:
\itemize{
\item \code{"propagate"} (default): any \code{NULL} source produces a \code{NULL} result for
that row. Safe for sib-group labels where a missing parent ID should
produce no group, not a partial label.
\item \code{"skip"}: \code{NULL} sources are silently omitted; remaining non-\code{NULL}
values are concatenated. A row where all sources are \code{NULL} still yields
a \code{NULL} result.
\item \code{"literal"}: \code{NULL} sources are replaced with the string \code{"NA"} before
concatenation.
}
}

\examples{
\dontrun{
# Contemporary group: sex × line_name
pop <- pop |>
  get_table("ind_meta") |>
  mutate_group_concatenate(
    col_name       = "cg",
    source_columns = c("sex", "line_name")
  )

# Sib group from parent IDs — NULL propagates when a parent is unknown
pop <- pop |>
  get_table("ind_meta") |>
  mutate_group_concatenate(
    col_name       = "sib_group",
    source_columns = c("id_parent_1", "id_parent_2"),
    null_action    = "propagate"
  )

# Generation × sex label, NULLs as literal "NA"
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 1L) |>
  mutate_group_concatenate(
    col_name       = "gen_sex",
    source_columns = c("gen", "sex"),
    sep            = ":",
    null_action    = "literal"
  )
}
}
\seealso{
\code{\link[=mutate_group_seq]{mutate_group_seq()}}, \code{\link[=mutate_group_named]{mutate_group_named()}}, \code{\link[=mutate_table]{mutate_table()}}
}
