% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_table.R
\name{define_table}
\alias{define_table}
\title{Define a custom table in the population database}
\usage{
define_table(pop, table_name, ..., primary_key = NULL, overwrite = FALSE)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{table_name}{Character scalar. Name for the new table. Must be a valid
SQL identifier and must not conflict with a system-managed tidybreed table.}

\item{...}{Named column definitions. Each name becomes a column; the value's
R type determines the DuckDB column type. Use typed NAs to declare columns
without supplying real data (e.g. \code{run_id = NA_integer_}).}

\item{primary_key}{Character scalar (optional). Name of one of the \code{...}
columns to declare as the \verb{PRIMARY KEY}. When supplied, \code{mutate_table()}
filtered-row updates will work on this table (DuckDB enforces uniqueness).
Defaults to \code{NULL} (no primary key).}

\item{overwrite}{Logical. If \code{FALSE} (default), an error is raised when
\code{table_name} already exists. If \code{TRUE}, the existing table is dropped and
recreated (all data in it will be lost).}
}
\value{
The \code{tidybreed_pop} (invisibly). Assign the result back.
}
\description{
Creates a new user-defined table inside the tidybreed DuckDB database and
registers it in \code{pop$tables} so that \code{\link[=get_table]{get_table()}} and \code{\link[=mutate_table]{mutate_table()}} work
on it immediately.

Column names and types are specified as named \code{...} arguments using the same
typed-NA convention as \code{\link[=mutate_table]{mutate_table()}} schema pre-declaration:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pop |> define_table(
  "sim_timing",
  run_id       = NA_integer_,
  duration_sec = NA_real_,
  label        = NA_character_
)
}\if{html}{\out{</div>}}

Types are inferred by \code{\link[=infer_duckdb_type]{infer_duckdb_type()}}. Use R's typed NA values
(\code{NA_integer_}, \code{NA_real_}, \code{NA_character_}, \code{as.Date(NA)}, etc.) to get the
intended DuckDB column type. There is no typed logical NA in R, so for a
\code{BOOLEAN} column pass a concrete placeholder (\code{FALSE} or \code{TRUE}) instead.
Bare \code{NA} defaults to \code{VARCHAR} with a warning.

After creation, add rows via \code{DBI::dbAppendTable(pop$db_conn, table_name, my_tibble)} and query via \code{get_table(pop, table_name) |> dplyr::collect()}.
}
\examples{
\dontrun{
pop <- open_pop(pop_name = "demo", db_name = ":memory:") |>
  define_genome(n_loci = 200, n_chr = 2, chr_len_Mb = 100)

# Create a custom table for tracking simulation run metadata
pop <- pop |> define_table(
  "sim_timing",
  run_id       = NA_integer_,
  duration_sec = NA_real_,
  label        = NA_character_,
  primary_key  = "run_id"
)

# Insert rows directly via DBI
DBI::dbAppendTable(
  pop$db_conn, "sim_timing",
  tibble::tibble(run_id = 1L, duration_sec = 42.3, label = "baseline")
)

# Query via the tidy interface
get_table(pop, "sim_timing") |> dplyr::collect()

# Add more columns later
pop <- pop |> get_table("sim_timing") |> mutate_table(notes = NA_character_)

close_pop(pop)
}
}
\seealso{
\code{\link[=get_table]{get_table()}}, \code{\link[=mutate_table]{mutate_table()}}
}
