% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_group_seq.R
\name{mutate_group_seq}
\alias{mutate_group_seq}
\title{Assign sequential integer group IDs to filtered rows}
\usage{
mutate_group_seq(
  tbl,
  col_name,
  n_per_group = NULL,
  n_groups = NULL,
  start = NULL,
  include_leftover = FALSE,
  seed = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{get_table()} (optionally filtered).}

\item{col_name}{Character scalar. Name of the group column to create or
update. Must be a valid SQL identifier and not a reserved column.}

\item{n_per_group}{Integer scalar or length-2 integer vector \code{c(min, max)}.
Mutually exclusive with \code{n_groups}.}

\item{n_groups}{Positive integer scalar. Split filtered rows into this many
groups. Mutually exclusive with \code{n_per_group}.}

\item{start}{Integer scalar. First group label. Default: \code{MAX(col_name) + 1}
if the column exists, else \code{1}.}

\item{include_leftover}{Logical. If \code{TRUE}, remainder animals are assigned to
a final (smaller) group. If \code{FALSE} (default), they are left \code{NULL}.}

\item{seed}{Integer or \code{NULL}. If supplied, sets the RNG seed before
shuffling so results are reproducible. Validation happens before the seed
is set, so a failed call does not consume RNG state.}

\item{overwrite}{Logical. If \code{FALSE} (default), error when any filtered row
already has a non-\code{NULL} value in \code{col_name}.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages.}
}
\value{
The modified \code{tidybreed_pop} object (invisibly).
}
\description{
Randomly shuffles the filtered rows and assigns them to consecutive integer
group labels (pen 1, 2, 3 …). The column is always \code{INTEGER}. Useful for
pen assignment, contemporary group numbering, and any situation where each
group is identified by a unique integer.

Pipe \code{get_table("ind_meta")} — optionally through \code{dplyr::filter()} — into
this function. Filtered primary keys are sorted ascending before shuffling,
giving a stable, reproducible starting point.
}
\section{Group-size modes}{

Exactly one of \code{n_per_group} or \code{n_groups} must be supplied.
\itemize{
\item \strong{\code{n_per_group} scalar}: every group has exactly \code{n_per_group} animals.
Remainder animals are left \code{NULL} unless \code{include_leftover = TRUE}.
\item \strong{\code{n_per_group} length-2 vector \code{c(min, max)}}: each group is sized by
drawing uniformly from \code{min:max}. When remaining animals fall below \code{min},
they are left \code{NULL} (or included if \code{include_leftover = TRUE}).
\item \strong{\code{n_groups}}: splits the filtered set into \code{n_groups} groups of size
\code{floor(n / n_groups)}. When \code{include_leftover = TRUE}, the first
\code{n \%\% n_groups} groups each receive one extra animal (balanced split).
When \code{include_leftover = FALSE}, the remainder animals are left \code{NULL}.
}
}

\section{Start numbering}{

When \code{start = NULL} (default), the next label is \code{MAX(col_name) + 1} if the
column already exists, or \code{1} for a new column. Supplying \code{start = 1L} resets
numbering — useful when assigning pens independently per generation.
Setting \code{start} does \strong{not} require \code{overwrite = TRUE}; overwrite only
applies when \strong{filtered rows} already hold non-\code{NULL} values.
}

\examples{
\dontrun{
# Assign males to pens of 10 (remainder left NULL)
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(sex == "M") |>
  mutate_group_seq(col_name = "pen", n_per_group = 10L)

# Assign all individuals to 5 balanced groups
pop <- pop |>
  get_table("ind_meta") |>
  mutate_group_seq(col_name = "pen", n_groups = 5L, include_leftover = TRUE)

# Restart pen numbering from 1 for generation 2 (no overwrite = TRUE needed)
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 2L) |>
  mutate_group_seq(col_name = "pen", n_per_group = 10L, start = 1L)

# Reproducible assignment
pop <- pop |>
  get_table("ind_meta") |>
  mutate_group_seq(col_name = "pen", n_per_group = 10L, seed = 42L)
}
}
\seealso{
\code{\link[=mutate_group_named]{mutate_group_named()}}, \code{\link[=mutate_group_concatenate]{mutate_group_concatenate()}}, \code{\link[=mutate_table]{mutate_table()}}
}
