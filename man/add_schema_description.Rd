% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{add_schema_description}
\alias{add_schema_description}
\title{Add or update a description for a table or column}
\usage{
add_schema_description(
  pop,
  table_name,
  description,
  column_name = NULL,
  notes = NULL
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{table_name}{Character. Name of the table.}

\item{description}{Character. Human-readable description.}

\item{column_name}{Character or \code{NULL}. When \code{NULL} (default), the
description applies to the table itself. When a column name is given,
the description applies to that specific column.}

\item{notes}{Character or \code{NULL}. Optional supplementary context.}
}
\value{
\code{pop}, invisibly.
}
\description{
Upserts a description into \verb{_schema_meta}. Use this to document custom
columns added via \code{mutate_table()} or \code{...} in \code{add_founders()} etc.
}
\examples{
\dontrun{
# Document a custom column added via mutate_table()
pop <- pop |>
  get_table("ind_meta") |>
  mutate_table(gen = NA_integer_)
pop <- add_schema_description(pop, "ind_meta",
                              "Generation number (0 = founders)",
                              column_name = "gen")
describe_table(pop, "ind_meta")
}
}
\seealso{
\code{\link[=describe_table]{describe_table()}}, \code{\link[=schema]{schema()}}
}
