% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_residual_cov.R
\name{set_residual_cov}
\alias{set_residual_cov}
\title{Store a residual covariance matrix across traits}
\usage{
set_residual_cov(pop, traits, R, tol = 1e-06)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{traits}{Character vector of trait names (length >= 2).}

\item{R}{Numeric matrix, square with side = \code{length(traits)}, symmetric
positive semi-definite.}

\item{tol}{Numeric tolerance for diagonal match (default \code{1e-6}).}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Writes the off-diagonal and diagonal entries of a residual covariance
matrix \code{R} to the \code{trait_residual_cov} table. Consumed by \code{\link[=add_phenotype]{add_phenotype()}}
when multiple traits are phenotyped on the same filtered subset, causing
residuals to be drawn jointly from MVN(0, R) instead of independently.

Diagonal entries of \code{R} must match each trait's stored \code{residual_var}
(within a small tolerance); otherwise an error is raised. This keeps
\code{trait_meta} and \code{trait_residual_cov} consistent.

Previous entries involving any of the listed traits are overwritten.
}
\examples{
\dontrun{
R <- matrix(c(0.75, 0.20, 0.20, 0.70), 2, 2)
pop <- pop |>
  set_residual_cov(traits = c("ADG", "BW"), R = R)
}
}
\seealso{
\code{\link[=set_qtl_effects_multi]{set_qtl_effects_multi()}}, \code{\link[=add_phenotype]{add_phenotype()}}
}
