% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{.schema_table_order}
\alias{.schema_table_order}
\title{Display groups for \code{\link[=schema]{schema()}}, in pipeline order}
\usage{
.schema_table_order()
}
\value{
A named list of character vectors, in display order.
}
\description{
Option A from \code{plans/update_schema_print.md}: a hard-coded vector rather than
a name-prefix rule or a \verb{_schema_meta} column. A table added later and not
registered here degrades \emph{visibly} — it appears under \strong{User tables} — rather
than being silently misfiled by a lexical rule that cannot tell
\code{ind_haplotype} (raw genome data) from \code{ind_tbv} (simulation output).
}
\details{
In-group order is workflow order, not alphabetical. \code{"User tables"} is an
intentionally empty slot: unrecognized tables land there, sorted by name, and
\code{"System"} always prints last.

Must name the same tables as the \verb{.\\*_descriptions()} helpers above.
}
\keyword{internal}
