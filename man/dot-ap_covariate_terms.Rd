% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotype_helpers.R
\name{.ap_covariate_terms}
\alias{.ap_covariate_terms}
\title{Stage-1 covariate terms of one phenotype: fixed contributions and the
random-effect level every record touches}
\usage{
.ap_covariate_terms(pop, phenotype_name, subset_df)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{phenotype_name}{Character. Phenotype name to look up in \code{phenotype_effects}.}

\item{subset_df}{Data frame: the per-phenotype subset of \code{ind_meta} (already
sex-filtered). Must contain \code{id_ind} and any \code{ind_meta} columns referenced
by effects.}
}
\value{
A list:
\itemize{
\item \code{fixed}: numeric vector of length \code{nrow(subset_df)}; \code{NA} for
individuals excluded by \code{null_class_action = "skip"}.
\item \code{random}: a list, one element per random effect in \code{effect_name}
order, each \code{list(effect_name, distribution, level)} where \code{level} is
a character vector of length \code{nrow(subset_df)} (\code{NA} = no level).
}
}
\description{
No RNG, no writes. Fixed-class and fixed-covariate effects are evaluated to
a per-record contribution, with \code{null_class_action = "skip"} marking the
record \code{NA}. Random effects are \strong{not} drawn here: the function records
which level of the grouping column each record falls in so that Stage 2
can resolve the draws once the record list is final (see
\link{add_phenotype_stages}).
}
\keyword{internal}
