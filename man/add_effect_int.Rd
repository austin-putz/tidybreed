% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_effect_int.R
\name{add_effect_int}
\alias{add_effect_int}
\title{Set the intercept (overall mean) for a trait}
\usage{
add_effect_int(pop, trait_name, mean)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{trait_name}{Character. Name of an existing trait.}

\item{mean}{Numeric scalar. The new intercept value.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Updates \code{target_add_mean} in \code{trait_meta} for the named trait. This value
is the intercept added to every individual's liability before residuals and
fixed/random shifts. It can also be set at trait-definition time via
\code{\link[=add_trait]{add_trait()}}; \code{add_effect_int()} lets you update it without redefining
the whole trait.
}
\examples{
\dontrun{
pop <- pop |> add_effect_int("ADG", mean = 850)
}
}
\seealso{
\code{\link[=add_trait]{add_trait()}}, \code{\link[=add_effect_fixed_class]{add_effect_fixed_class()}}, \code{\link[=add_effect_fixed_cov]{add_effect_fixed_cov()}},
\code{\link[=add_effect_random]{add_effect_random()}}
}
