% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_phenotype_stages.R
\name{.ap_resolve_named_effects}
\alias{.ap_resolve_named_effects}
\title{The named-effect adapter: every random-effect draw of the call}
\usage{
.ap_resolve_named_effects(plan)
}
\arguments{
\item{plan}{The Stage-1 plan.}
}
\value{
A list with \code{contribution} (named by model-path phenotype with
planned records: the summed random-effect value per record, \code{0} for a
record whose level is \code{NULL}) and \code{pending} (the new
\code{phenotype_random_effects} rows for Stage 3).
}
\description{
Implements \code{plans/sample_correlated_effects.md} §5.6 and §5.8 for every
\code{effect_name} other than \code{'residual'}. The entity is the \emph{level} — a pen,
a herd, an \code{id_ind} for a permanent-environment effect — and a level's
draw is realized once and reused by every record that ever touches it,
in this call or any later one. Effects are processed in byte-sorted
order, each through its blocks (from the plan, in
\code{\link[=find_covariance_blocks]{find_covariance_blocks()}} order): a level draws its planned coordinates
conditional on the coordinates already stored in
\code{phenotype_random_effects} for the block's other phenotypes. Every
model-path phenotype with a random term for the effect must be in a
block (else "No variance stored"); the §5.6 checks are re-run here as
the backstop; a 1 x 1 block whose effect is \code{gamma} or \code{uniform} keeps
its marginal sampler.
}
\keyword{internal}
