% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{.schema_table_bytes}
\alias{.schema_table_bytes}
\title{Per-table on-disk size, in bytes}
\usage{
.schema_table_bytes(conn, tables)
}
\arguments{
\item{conn}{A DBI connection.}

\item{tables}{Character vector of table names.}
}
\value{
Numeric vector of bytes, aligned to \code{tables}; \code{NA} where the pragma
could not be read.
}
\description{
DuckDB has no direct per-table byte count. \verb{duckdb_tables().estimated_size}
looks right but is estimated \emph{cardinality}, not bytes. The workable route is
\verb{PRAGMA storage_info()}, counting the distinct blocks a table's segments
occupy and multiplying by the database block size.
}
\details{
The caller must \code{CHECKPOINT} first: before one, recently written data carries
\code{block_id = -1} and every table reports zero. That is a write, which is why
\code{sizes} is opt-in on \code{\link[=schema]{schema()}} rather than always on.
}
\keyword{internal}
