% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haplotype_write_helpers.R
\name{.write_crossovers}
\alias{.write_crossovers}
\title{Insert a batch of crossover events into \code{ind_crossover}}
\usage{
.write_crossovers(conn, xframe)
}
\arguments{
\item{conn}{DuckDB connection.}

\item{xframe}{data.frame with columns \code{id_ind}, \code{parent_origin}, \code{chr},
\code{chr_name}, \code{pos_cM} (one row per crossover event).}
}
\value{
Invisibly \code{NULL}.
}
\description{
Assigns \code{id_crossover} R-side via the BIGINT-safe \code{\link[=next_row_id]{next_row_id()}} (the table
can accumulate far more rows than any INTEGER-keyed table), then registers the
frame and does one \code{INSERT}. RNG-neutral (register + INSERT). Called only when
\code{add_offspring(store_crossovers = TRUE)}; within a transaction, \code{next_row_id()}
sees prior batches' rows, so ids stay sequential across batches.
}
\keyword{internal}
