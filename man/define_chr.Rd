% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_chr.R
\name{define_chr}
\alias{define_chr}
\title{Define a chromosome's inheritance rule}
\usage{
define_chr(
  pop,
  chr_name,
  copy_mode_M = "full",
  copy_mode_F = "full",
  hemi_parent = NULL,
  recombines = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{chr_name}{Character scalar. Must already exist as a
\code{genome_meta.chr_name} value (defined via \code{\link[=define_genome]{define_genome()}}'s
\code{chr_names} argument).}

\item{copy_mode_M}{Character scalar: \code{"full"} (default), \code{"half"}, or
\code{"none"}. Copy count for males, relative to ploidy.}

\item{copy_mode_F}{Character scalar: \code{"full"} (default), \code{"half"}, or
\code{"none"}. Copy count for females, relative to ploidy.}

\item{hemi_parent}{\code{NULL}, \code{"parent_1"}, or \code{"parent_2"}. Which parent
supplies the reduced copy when either sex's copy_mode is not \code{"full"}.
Must be \code{NULL} when both copy_modes are \code{"full"}; must be
\code{"parent_1"}/\code{"parent_2"} otherwise.}

\item{recombines}{Logical. \code{TRUE} (default) if recombination occurs during
gamete formation; \code{FALSE} for non-recombining chromosomes (Y, W, MT, most
organelles).}

\item{overwrite}{Logical. If \code{TRUE} (default), re-calling \code{define_chr()}
for the same \code{chr_name} updates the existing row in place — this is a
normal edit workflow (e.g. fixing a typo'd \code{hemi_parent}), not append-only
simulation output, so the default favors correction over silent no-op.}
}
\value{
The \code{tidybreed_pop} (invisibly). Assign the result back.
}
\description{
Sets a non-default inheritance rule for one chromosome in \code{chr_meta} — sex
chromosomes (X/Y, Z/W, X0/Z0) and organelles (mitochondria, plastids).
\code{initialize_genome()}/\code{define_genome()} seeds every chromosome with the
default autosome rule (\code{copy_mode_M = copy_mode_F = "full"},
\code{recombines = TRUE}); call \code{define_chr()} before \code{\link[=add_founders]{add_founders()}} to
override that default for a specific chromosome.

\code{copy_mode_M}/\code{copy_mode_F} are \strong{relative to an individual's own ploidy},
not an absolute copy count: \code{"full"} means the same as that individual's
ploidy for this chromosome, \code{"half"} means hemizygous (half of that
ploidy), and \code{"none"} means absent. In this version of tidybreed every
individual's ploidy is \code{2}, so \code{"full"} = 2 copies and \code{"half"} = 1 copy.
}
\examples{
\dontrun{
# Mammalian sex chromosomes
pop <- pop |>
  define_chr("X", copy_mode_M = "half", copy_mode_F = "full",
             hemi_parent = "parent_2", recombines = TRUE) |>
  define_chr("Y", copy_mode_M = "half", copy_mode_F = "none",
             hemi_parent = "parent_1", recombines = FALSE)

# Mitochondria (maternal, non-recombining)
pop <- pop |>
  define_chr("MT", copy_mode_M = "half", copy_mode_F = "half",
             hemi_parent = "parent_2", recombines = FALSE)
}
}
\seealso{
\code{\link[=define_genome]{define_genome()}}
}
