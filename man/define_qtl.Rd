% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_qtl.R
\name{define_qtl}
\alias{define_qtl}
\title{Define QTL loci for a trait}
\usage{
define_qtl(
  pop,
  trait_name,
  n = NULL,
  method = "random",
  locus_tf = NULL,
  locus_ids = NULL,
  locus_names = NULL,
  col_name = paste0("is_QTL_", trait_name)
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{trait_name}{Character. Name of an existing trait in \code{trait_meta}.}

\item{n}{Integer. Number of QTL to select (with \code{method}). Mutually
exclusive with \code{locus_tf}, \code{locus_ids}, \code{locus_names}.}

\item{method}{Character. Selection method when using \code{n}: \code{"random"}
(default), \code{"even"}, or \code{"chromosome_even"}.}

\item{locus_tf}{Logical. TRUE/FALSE vector of length \code{n_loci} indicating
QTL membership.}

\item{locus_ids}{Integer vector of locus IDs to mark as QTL.}

\item{locus_names}{Character vector of locus names to mark as QTL.}

\item{col_name}{Character. Column name in \code{genome_meta}. Default:
\code{paste0("is_QTL_", trait_name)}.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Marks a subset of loci as QTL for a named trait by writing a BOOLEAN
column \verb{is_QTL_\{trait\}} to \code{genome_meta}. Mirrors \code{\link[=define_chip]{define_chip()}}: the same
six selection methods are supported (by count + \code{method}, by logical
vector, by locus IDs, by locus names).

The trait must already exist in \code{trait_meta} (see \code{\link[=add_trait]{add_trait()}}).
}
\examples{
\dontrun{
pop <- pop |>
  add_trait("ADG", target_add_var = 0.25, residual_var = 0.75) |>
  define_qtl("ADG", n = 100, method = "chromosome_even")
}
}
\seealso{
\code{\link[=define_chip]{define_chip()}}, \code{\link[=set_qtl_effects]{set_qtl_effects()}}, \code{\link[=add_trait]{add_trait()}}
}
