% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlated_draws.R
\name{find_covariance_blocks}
\alias{find_covariance_blocks}
\title{Load the covariance block(s) touching a set of phenotypes}
\usage{
find_covariance_blocks(conn, effect_name, phenotype_names)
}
\arguments{
\item{conn}{A DBI connection.}

\item{effect_name}{\code{"residual"} or a named random effect.}

\item{phenotype_names}{Character vector of the phenotypes whose blocks are
wanted.}
}
\value{
A list of blocks, ordered by their first member; possibly empty.
Each block is a list with:
\describe{
\item{\code{effect_name}}{as supplied.}
\item{\code{phenotypes}}{sorted character vector of members.}
\item{\code{condition_table}, \code{condition_column}}{the block's condition
column, or \code{NULL} when it has only the unconditional stratum.}
\item{\code{unconditional}}{the unconditional matrix, or \code{NULL} if none is
stored.}
\item{\code{conditional}}{a list of matrices named by \code{condition_level}
(empty when there are no conditional strata).}
}
Every matrix is \code{phenotypes} x \code{phenotypes} with dimnames, exactly
symmetric, in the sorted member order.
}
\description{
A \emph{block} is a connected component of the graph whose edges are stored pair
rows in \code{phenotype_var_comp} for \code{effect_name} (a \code{cov_value} of \code{0} is an
edge). The requested phenotypes may fall into several components; each is
returned with every stratum stored for it. A phenotype with no row at all
belongs to no block and is simply absent from the result — callers decide
what that means (for the residual effect it is "no residual variance").
}
\details{
The writers in \code{R/phenotype_cov_block.R} guarantee that every stratum of a
block is a complete, symmetric matrix over the same phenotypes and that a
block has one \verb{(condition_table, condition_column)}. Rows can still be
removed by hand with \code{remove_rows()}, so the loader re-checks those
invariants and errors, naming the block, when they no longer hold.
}
\keyword{internal}
