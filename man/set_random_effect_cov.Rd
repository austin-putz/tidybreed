% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_random_effect_cov.R
\name{set_random_effect_cov}
\alias{set_random_effect_cov}
\title{Store a covariance matrix for correlated random effects across traits}
\usage{
set_random_effect_cov(
  pop,
  effect_name,
  traits,
  R,
  tol = 1e-06,
  overwrite = FALSE
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{effect_name}{Character. The effect name shared across traits (must
exist in \code{trait_effects} for each trait listed in \code{traits}).}

\item{traits}{Character vector of at least two trait names.}

\item{R}{Numeric matrix of size \verb{length(traits) x length(traits)}. Must be
symmetric. Rows and columns are matched to \code{traits} in order.}

\item{tol}{Numeric. Tolerance for symmetry and diagonal-variance checks.}

\item{overwrite}{Logical. Replace existing entries for this
\verb{(effect_name, traits)} combination.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Saves a covariance matrix \code{R} into \code{trait_random_effect_cov}, enabling
\code{\link[=add_phenotype]{add_phenotype()}} to draw the named random effect jointly across traits via
\code{MVN(0, R)}. The diagonal of \code{R} must match the \code{variance} stored in
\code{trait_effects} for each \verb{(trait, effect_name)} pair (within \code{tol}).

Analogous to \code{\link[=set_residual_cov]{set_residual_cov()}} but for named group-level random effects.
}
\examples{
\dontrun{
pop <- pop |>
  add_effect_random("ADG", "herd", source_column = "herd_id", variance = 150) |>
  add_effect_random("BW",  "herd", source_column = "herd_id", variance = 100) |>
  set_random_effect_cov("herd",
    traits = c("ADG", "BW"),
    R = matrix(c(150, 60, 60, 100), 2, 2))
}
}
\seealso{
\code{\link[=add_effect_random]{add_effect_random()}}, \code{\link[=set_residual_cov]{set_residual_cov()}}, \code{\link[=add_phenotype]{add_phenotype()}}
}
