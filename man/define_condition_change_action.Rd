% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_condition_change_action.R
\name{define_condition_change_action}
\alias{define_condition_change_action}
\title{Set \code{condition_change_action} for a whole residual covariance block}
\usage{
define_condition_change_action(
  pop,
  phenotype_name,
  condition_change_action = c("error", "independent")
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop}.}

\item{phenotype_name}{Character scalar. Any member of the block; the value
is written to every member.}

\item{condition_change_action}{\code{"error"} (stop when a stored residual comes
from another stratum) or \code{"independent"} (drop it from the conditioning
set and warn).}
}
\value{
The \code{tidybreed_pop}, invisibly.
}
\description{
\code{condition_change_action} decides what \code{\link[=add_phenotype]{add_phenotype()}} does when a
correlated phenotype's stored residual was drawn under a different residual
\code{condition_level} than the current record resolves to — an animal that moved
farms between two records, say. It is a property of the \strong{residual
covariance block}, not of one phenotype: D6 requires every member of a
block to carry the same value, so this function is the only way to change it
once a block of two or more exists.
}
\details{
\code{\link[=define_phenotype]{define_phenotype()}} sets the value at registration time, when the phenotype
is still a block of one. Afterwards the two are locked together — flipping
either one alone through \code{define_phenotype(overwrite = TRUE)} would leave
the block disagreeing, which D6 refuses, and restating the phenotype would
reset every other column of its \code{phenotype_meta} row along the way. This
function changes exactly one column, on every member, in one transaction.

Unlike the covariance matrix itself, the action is \strong{not} locked by
realized draws (D3). It governs how \emph{future} records condition on stored
residuals and says nothing about the ones already drawn, so changing it
mid-simulation is legitimate and leaves \code{ind_phenotype} and
\code{phenotype_random_effects} untouched.
}
\examples{
\dontrun{
# A and B share a farm-conditioned residual block, both at the default
# "error". An animal that changes farms between records should now be
# drawn independently of its stale residual rather than stopping the run.
pop <- define_condition_change_action(pop, "A", "independent")
# -> also set on B (same residual covariance block)
}
}
\seealso{
\code{\link[=define_phenotype]{define_phenotype()}}, \code{\link[=define_residual_cov]{define_residual_cov()}}, \code{\link[=add_phenotype]{add_phenotype()}}
}
