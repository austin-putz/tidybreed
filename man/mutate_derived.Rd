% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_derived.R
\name{mutate_derived}
\alias{mutate_derived}
\title{Compute and write a derived column from a cross-table join}
\usage{
mutate_derived(
  tbl_obj,
  compute,
  join_table = NULL,
  join_by = "id_ind",
  write_to
)
}
\arguments{
\item{tbl_obj}{A \code{tidybreed_table} returned by \code{\link[=get_table]{get_table()}} with an
optional \code{\link[=filter]{filter()}} applied.}

\item{compute}{A function \verb{function(df) -> vector}. \code{df} is the joined
tibble (primary table left-joined to \code{join_table}). The returned vector
must be a scalar (broadcast to all rows) or have the same length as
\code{nrow(df)}. Primary table column names take precedence on name collisions;
any colliding column from \code{join_table} is suffixed with \code{".<join_table>"}.}

\item{join_table}{Character scalar or \code{NULL}. Name of a secondary table to
left-join with the primary table. Must exist in \code{pop$tables}. \code{NULL}
(default) skips the join; \code{compute} receives only the primary table rows.}

\item{join_by}{Character scalar. Column name to join on; must exist in both
tables when \code{join_table} is not \code{NULL}. Default \code{"id_ind"}.}

\item{write_to}{Named character vector. Names are destination table names;
values are destination column names. Example:
\code{c(ind_meta = "puberty_date", ind_phenotype = "pheno_date")}.
Each destination table must be registered in the package primary-key map
(\code{TABLE_PRIMARY_KEYS}). Reserved columns are blocked.}
}
\value{
The parent \code{tidybreed_pop} object (invisibly).
}
\description{
A pipe-friendly action function that:
\enumerate{
\item Collects the current (optionally filtered) primary table
\item Left-joins an optional secondary table by \code{join_by}
\item Applies a user-supplied \code{compute} function to produce a derived vector
\item Writes the result to one or more destination columns across any tables
}

This reduces ~25-line dplyr join + \code{mutate_table()} workflows for computed
fields (e.g. \code{puberty_date = birth_date + AP_value}) to a single pipe step.
}
\details{
\subsection{Writing back to the primary table}{

Uses the table's own primary key for exact-row matching. A filtered
\code{ind_phenotype} (e.g. \code{filter(trait_name == "AP")}) only updates those
specific rows — other trait records for the same individual are untouched.
}

\subsection{Writing to a secondary table}{

Requires at most one computed value per \code{join_by} ID. If the primary table
has multiple rows for the same individual that produce \emph{different} computed
values, \code{mutate_derived()} errors with a message asking you to pre-filter.
Identical values for the same \code{join_by} ID are silently deduplicated (first
occurrence wins).
}

\subsection{Column name collisions during the join}{

If the primary and secondary tables share a column name other than \code{join_by},
the secondary table's copy gains a \code{".<join_table>"} suffix inside \code{compute}.
Primary table columns are always unambiguous (no suffix).
}

\subsection{join_table row expansion}{

If \code{join_table} itself contains multiple rows per \code{join_by} value, the
left-join silently expands the primary table rows. Pre-filter \code{join_table}
externally before passing it if this is a concern.
}

\subsection{New column type inference}{

When a destination column does not yet exist, its DuckDB type is inferred
from \code{result_vec} via \code{infer_duckdb_type()}. R's \code{Date} arithmetic preserves
the \code{Date} class, so \code{birth_date + integer} correctly produces a \code{DATE}
column. Use typed \code{NA} vectors (e.g. \code{NA_real_}) if \code{result_vec} is all-NA.
}
}
\examples{
\dontrun{
# Age-at-puberty: puberty_date = birth_date (ind_meta) + AP value (days)
pop <- pop |>
  get_table("ind_phenotype") |>
  dplyr::filter(trait_name == "AP", pheno_number == 1L) |>
  mutate_derived(
    compute    = \(df) df$birth_date + as.integer(df$value),
    join_table = "ind_meta",
    join_by    = "id_ind",
    write_to   = c(ind_meta = "puberty_date", ind_phenotype = "pheno_date")
  )

# Compute entirely from the primary table (no join needed)
pop <- pop |>
  get_table("ind_phenotype") |>
  dplyr::filter(trait_name == "BW") |>
  mutate_derived(
    compute  = \(df) df$value * 2.205,   # kg -> lb
    write_to = c(ind_phenotype = "value_lb")
  )
}

}
