% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql_utils.R
\name{prepare_extra_cols}
\alias{prepare_extra_cols}
\title{Validate, type-infer, and expand extra user columns for pre-insert attachment}
\usage{
prepare_extra_cols(extra_cols, n_rows, table_name, conn)
}
\arguments{
\item{extra_cols}{Named list of values (captured from \code{list(...)}).}

\item{n_rows}{Integer. Number of rows in the insertion data frame.}

\item{table_name}{Character. Target table name.}

\item{conn}{DuckDB connection.}
}
\value{
Named list of expanded value vectors, one per field.
}
\description{
Called by \code{add_founders()}, \code{add_offspring()}, \code{add_phenotype()},
\code{add_tbv()}, and \code{add_ebv()} to process the \code{...} (extra field) arguments
before they are attached to an insertion data frame.
}
\details{
Scalars are broadcast to \code{n_rows}. Vectors must already have length \code{n_rows}.
Any column that does not yet exist in the target table is added via
\verb{ALTER TABLE ADD COLUMN} before the caller writes the data frame.
}
\keyword{internal}
