% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_genome_meta.R
\name{mutate_genome_meta}
\alias{mutate_genome_meta}
\title{Add or modify columns in genome metadata table}
\usage{
mutate_genome_meta(pop, ...)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object}

\item{...}{Named arguments specifying column names and values.
Each argument should be \code{field_name = value}, where value is either:
\itemize{
\item A scalar (applied to all loci)
\item A vector of length equal to number of loci
}}
}
\value{
The \code{tidybreed_pop} object (invisibly), modified in place
}
\description{
Adds new columns to the \code{genome_meta} table or updates existing custom columns.
Values can be scalar (applied to all loci) or vectors (one per locus).
The function automatically infers DuckDB column types from R data types.

Reserved columns (\code{locus_id}, \code{locus_name}, \code{chr}, \code{chr_name}, \code{pos_Mb}) cannot be modified.
}
\details{
\strong{Type Inference:}
\itemize{
\item \code{logical} → BOOLEAN
\item \code{integer} → INTEGER
\item \code{numeric} → DOUBLE
\item \code{Date} → DATE
\item \code{POSIXct}/\code{POSIXlt} → TIMESTAMP
\item \code{character} → VARCHAR
}

\strong{Field Naming Rules:}
\itemize{
\item Must start with a letter
\item Can contain letters, numbers, and underscores
\item Cannot be SQL reserved words
\item Cannot be reserved columns (locus_id, locus_name, chr, chr_name, pos_Mb)
}
}
\examples{
\dontrun{
# Initialize genome
pop <- initialize_genome(
  pop_name = "test",
  n_loci = 1000,
  n_chr = 10,
  chr_len_Mb = 100
)

# Add scalar fields (same value for all loci)
pop <- pop \%>\%
  mutate_genome_meta(
    is_QTL = FALSE,
    is_50k = FALSE
  )

# Add vector field (different value per locus)
n_loci <- 1000
pop <- pop \%>\%
  mutate_genome_meta(
    maf = runif(n_loci, 0.01, 0.5),
    is_functional = sample(c(TRUE, FALSE), n_loci, replace = TRUE)
  )

# Update existing field
pop <- pop \%>\%
  mutate_genome_meta(is_QTL = TRUE)

# View updated metadata
get_table(pop, "genome_meta") \%>\% collect()
}
}
