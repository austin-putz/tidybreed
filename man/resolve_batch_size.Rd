% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haplotype_write_helpers.R
\name{resolve_batch_size}
\alias{resolve_batch_size}
\title{Resolve the offspring/founders-per-batch size \code{B}}
\usage{
resolve_batch_size(
  n_total,
  n_loci,
  batch_size = NULL,
  max_batch_mem = NULL,
  bytes_per_offspring_row = 200,
  target_frac = 0.25,
  fallback_bytes = 512 * 1024^2
)
}
\arguments{
\item{n_total}{Integer. Total rows to process (offspring or founders).}

\item{n_loci}{Integer. Loci count (drives per-row memory).}

\item{batch_size}{Optional explicit batch size (wins when set).}

\item{max_batch_mem}{Optional explicit per-batch memory budget (bytes or
\code{"512MB"}-style string).}

\item{bytes_per_offspring_row}{Heuristic peak bytes per (offspring x locus)
during the in-R long build + register. Calibrated (~200) from Phase-0/Stage-1
measurements: each offspring emits \verb{2 x n_loci} long rows (two haplotypes),
each ~90 bytes at peak including the \code{rbind}/register transient copy — so the
per-(offspring x locus) constant is ~2 x 90 ~ 190, rounded up for headroom.}

\item{target_frac}{Fraction of available memory to target for one batch.}

\item{fallback_bytes}{Conservative per-batch budget when detection fails.}
}
\value{
Integer \code{B} in \verb{[1, n_total]} (or \code{0L} when \code{n_total == 0}).
}
\description{
Precedence: explicit \code{batch_size} > explicit \code{max_batch_mem} > RAM-aware
auto-pick (target \code{target_frac} of detected available memory) > conservative
fixed fallback (\code{fallback_bytes}) when detection fails. Always clamped to
\verb{[1, n_total]}. Any \code{B} yields byte-identical output — this only bounds peak
memory, never changes values.
}
\keyword{internal}
