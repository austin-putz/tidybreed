% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open_pop.R
\name{.create_run_dir}
\alias{.create_run_dir}
\title{Create a timestamped layer-5 run directory inside a tool dir}
\usage{
.create_run_dir(pop, tool, keep = "on_error")
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object with \code{run_dirs} populated.}

\item{tool}{Character scalar. Which tool dir to use (must be a key in
\code{pop$run_dirs}).}

\item{keep}{\code{FALSE}, \code{TRUE}, or \code{"on_error"}. Cleanup semantics (not yet
enforced automatically; reserved for a future release).}
}
\value{
Path to the newly created run directory (invisibly).
}
\description{
Called at the start of every external tool wrapper (e.g. \code{run_blupf90()},
\code{run_plink()}). Creates a unique subdirectory inside \code{pop$run_dirs[[tool]]}
for the current invocation and returns the path.

The tool must have been declared via the \code{tools} argument of \code{\link[=open_pop]{open_pop()}}.
If \code{tool} is not in \code{pop$run_dirs}, the function errors with a clear message
listing the registered tools.

For v0.40.0 the \code{keep} argument is accepted for API completeness but cleanup
is not yet automatically registered. The caller is responsible for deleting
the run directory on success if \code{keep = FALSE} or \code{keep = "on_error"}.
}
\keyword{internal}
