% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_founders.R
\name{add_founders}
\alias{add_founders}
\title{Add founder individuals to population}
\usage{
add_founders(tbl, n_males, n_females, line_name, ...)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{get_table("founder_haplotypes")}
(optionally piped through \code{dplyr::filter()}). The filtered rows supply
the haplotype pool for sampling; use the filter to select a line-specific
or custom subset.}

\item{n_males}{Integer. Number of male founders to create}

\item{n_females}{Integer. Number of female founders to create}

\item{line_name}{Character. Line identifier used for individual IDs.
IDs are formatted as \code{"{line_name}_{number}"} (e.g., "A_1", "A_2")}

\item{...}{Optional named arguments for custom \code{ind_meta} columns, e.g.
\code{gen = 0L}, \code{farm = "Iowa"}. Scalar values are broadcast to all new
founders; vectors must have length \code{n_males + n_females}. Column types
are inferred from the R type: use \code{0L} for INTEGER, \code{0} for DOUBLE,
\code{"text"} for VARCHAR, \code{TRUE}/\code{FALSE} for BOOLEAN. Reserved column names
(\code{id_ind}, \code{sex}, \code{line}, etc.) are blocked.}
}
\value{
The modified \code{tidybreed_pop} object (invisibly).
\strong{Important:} Assign the result back to update your object: \code{pop <- add_founders(pop, ...)}
}
\description{
Creates founder individuals by sampling haplotypes from the \code{founder_haplotypes}
table. Each founder receives two randomly sampled haplotypes (with replacement),
which are used to populate the \code{genome_haplotype} and \code{genome_genotype} tables.

The \code{ind_meta} table is created (if it doesn't exist) or appended to with
the new founders. Founder individuals have \code{NULL} for both parent IDs.
}
\details{
\strong{Requirements:}
\itemize{
\item The \code{founder_haplotypes} table must exist. Create it by calling
\code{initialize_genome()} with the \code{n_haplotypes} parameter.
}

\strong{What it does:}
\enumerate{
\item Samples 2 haplotypes per founder from \code{founder_haplotypes} (with replacement)
\item Creates/updates \code{ind_meta} table with founder metadata
\item Populates \code{genome_haplotype} (2 rows per individual)
\item Populates \code{genome_genotype} (1 row per individual, sum of haplotypes)
}

\strong{ID Format:}
\itemize{
\item Individual IDs: \code{"{line_name}_{number}"} (e.g., "A_1", "A_2", "B_1")
\item Numbers are sequential within each line
\item If founders already exist for a line, numbering continues from max ID
}

\strong{Multiple Lines:}
\itemize{
\item Can be called multiple times to add different lines to same database
\item Each line has independent ID numbering
}
}
\examples{
\dontrun{
pop <- open_pop(pop_name = "test", db_name = ":memory:") |>
  define_genome(n_loci = 1000, n_chr = 10, chr_len_Mb = 100) |>
  define_founder_haplotypes(n_haplotypes = 100, line_name = "A")

# Simple — all haplotypes in the table
pop <- pop |>
  get_table("founder_haplotypes") |>
  add_founders(n_males = 10, n_females = 100, line_name = "A")

# Filtered by line
pop <- pop |>
  get_table("founder_haplotypes") |>
  dplyr::filter(line_name == "Yorkshire") |>
  add_founders(n_males = 10, n_females = 50, line_name = "Yorkshire")

# With custom ind_meta columns via ...
pop <- pop |>
  get_table("founder_haplotypes") |>
  dplyr::filter(line_name == "A") |>
  add_founders(n_males = 10, n_females = 100, line_name = "A",
               gen = 0L, farm = "FarmA")

# Add a second line
pop <- pop |>
  get_table("founder_haplotypes") |>
  dplyr::filter(line_name == "B") |>
  add_founders(n_males = 5, n_females = 50, line_name = "B")

# View founders
get_table(pop, "ind_meta") |> dplyr::collect()
}
}
