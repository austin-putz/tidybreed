% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chr_meta_helpers.R
\name{resolve_chr_copy_count}
\alias{resolve_chr_copy_count}
\title{Resolve a chromosome's copy count for a given sex and ploidy}
\usage{
resolve_chr_copy_count(copy_mode, ploidy = 2L)
}
\arguments{
\item{copy_mode}{Character scalar: \code{"full"}, \code{"half"}, or \code{"none"}.}

\item{ploidy}{Integer scalar. The individual's own ploidy (always \code{2L} in
this version of tidybreed).}
}
\value{
Integer copy count (\code{0L}, \code{1L}, or \code{2L} in this version).
}
\description{
Translates \code{chr_meta}'s relative \code{copy_mode} (\code{"full"}/\code{"half"}/\code{"none"})
into an absolute copy count for one individual, given their own ploidy.
Shared by \code{\link[=add_founders]{add_founders()}} and \code{\link[=add_offspring]{add_offspring()}} so both agree on expected
row counts per chromosome per sex — duplicating this mapping inline in both
files would risk them silently disagreeing.
}
\keyword{internal}
