% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chr_meta_helpers.R
\name{assert_qtl_autosomal}
\alias{assert_qtl_autosomal}
\title{Error if any of the given loci sit on a non-"full" chromosome}
\usage{
assert_qtl_autosomal(conn, locus_names)
}
\arguments{
\item{conn}{A DBI connection.}

\item{locus_names}{Character vector of locus names to check.}
}
\value{
Invisible \code{NULL} on success; errors otherwise.
}
\description{
\code{rescale_effects_to_target()}'s Falconer variance formula
(\code{2 * p * (1-p) * a^2}) assumes every QTL is diploid/autosomal — it would
silently overstate the variance contribution of any QTL on a hemizygous
(\code{copy_mode != "full"}) locus, since a single-copy Bernoulli(p) draw has
genic variance \code{p*(1-p)*a^2}, not \code{2*p*(1-p)*a^2}. Rather than generalizing
the formula (an approximation that would need to average over the sexes
actually present in the population), \code{define_additive_effects()} fails
loudly instead when \code{scale_to_target = TRUE} and any selected locus is
sex-linked/organelle.
}
\keyword{internal}
