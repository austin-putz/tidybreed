% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_phenotype.R
\name{define_phenotype}
\alias{define_phenotype}
\title{Define an observed phenotype}
\usage{
define_phenotype(
  pop,
  phenotype_name,
  trait_type = c("continuous", "count", "categorical"),
  mean = 0,
  expressed_sex = c("both", "M", "F"),
  repeatable = FALSE,
  min_value = NULL,
  max_value = NULL,
  prevalence = NULL,
  thresholds = NULL,
  cat_values = NULL,
  cat_names = NULL,
  store_liability = FALSE,
  residual_var = NULL,
  components = NULL,
  missing_component_action = c("skip", "error"),
  overwrite = FALSE
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{phenotype_name}{Character. Name of the observed phenotype; equals
\code{trait_name} for simple (non-composite) traits.}

\item{trait_type}{Character. One of \code{"continuous"}, \code{"count"},
\code{"categorical"}.}

\item{mean}{Numeric. Phenotypic population mean (intercept). Default \code{0}.}

\item{expressed_sex}{Character. Who receives a phenotype record: \code{"both"}
(default), \code{"M"}, or \code{"F"}.}

\item{repeatable}{Logical. Whether an individual can have multiple records
for this phenotype (e.g. repeated test-day or litter-size records).
Default \code{FALSE}.}

\item{min_value, max_value}{Numeric. Clipping bounds for count traits.
\code{NULL} means no limit.}

\item{prevalence}{Numeric between 0 and 1. For categorical traits with one
threshold (two categories), the fraction expected above the threshold.
Mutually exclusive with \code{thresholds}.}

\item{thresholds}{Numeric vector of length K−1 for K ordered categories.
Liability cutpoints in ascending order. Mutually exclusive with
\code{prevalence}.}

\item{cat_values}{Numeric vector of length K. Phenotype value stored in
\code{ind_phenotype} for each category. Defaults to \verb{1, 2, ..., K}.}

\item{cat_names}{Character vector of length K. Human-readable label per
category, e.g. \code{c("Alive", "Dead")}. Must not contain commas.}

\item{store_liability}{Logical. When \code{TRUE}, the underlying liability value
is written to the reserved \code{liability_value} column in \code{ind_phenotype}.
Only meaningful for categorical traits.}

\item{residual_var}{Numeric. Scalar residual variance. When supplied, writes
one unconditional row to \code{phenotype_residual_cov}
(\code{condition_column = NULL}). For heterogeneous residuals or
multi-phenotype correlated residuals, use \code{\link[=define_residual_cov]{define_residual_cov()}}
afterwards.}

\item{components}{A data frame or \code{tibble} with one row per genetic
component. Columns:
\itemize{
\item \code{source_trait_name} (required): component trait name in \code{trait_meta}.
\item \code{contributor_type} (required): \code{"self"}, \code{"dam"}, \code{"sire"}, or
\code{"group"}.
\item \code{weight} (optional, default \code{1.0}): scalar multiplier.
\item \code{weight_type} (optional, default \code{"fixed"}): \code{"fixed"}, \code{"covariate"},
\code{"legendre"}, or \code{"raw_poly"}.
\item \code{covariate_name} (optional): covariate key.
\item \code{covariate_table} (optional): table containing the covariate column;
\code{NULL} means value supplied at \code{\link[=add_phenotype]{add_phenotype()}} call time.
\item \code{poly_order} (optional): polynomial basis order.
\item \code{poly_scale_min}, \code{poly_scale_max} (optional): Legendre scaling bounds.
\item \code{genome_effect_types} (optional, default \code{"additive"}).
\item \code{group_column} (optional): column defining group membership.
\item \code{group_table} (optional, default \code{"ind_meta"}): table containing
\code{group_column}.
\item \code{aggregation} (optional, default \code{"sum"}): \code{"sum"} or \code{"mean"} for
group contributors.
}

\code{NULL} (default) → simple single-self trait; \code{phenotype_components} not
written.}

\item{missing_component_action}{Character. What to do when an individual is
missing one or more required components (e.g. no group assignment, missing
dam TBV, missing random effect draw). \code{"skip"} (default) excludes the
individual from \code{ind_phenotype} and emits a warning with a count.
\code{"error"} stops with an informative message listing affected individuals.
Stored in \code{phenotype_meta} so the behaviour is consistent across all
\code{\link[=add_phenotype]{add_phenotype()}} calls for this phenotype.}

\item{overwrite}{Logical. If \code{TRUE} and a phenotype with the same name
already exists, replace its rows in \code{phenotype_meta} and
\code{phenotype_components}. Default \code{FALSE} errors on duplicate.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Registers an \strong{observed phenotype}: the quantity written to \code{ind_phenotype}
when \code{\link[=add_phenotype]{add_phenotype()}} is called. For simple (non-composite) traits,
\code{phenotype_name} equals \code{trait_name} and \code{\link[=define_trait]{define_trait()}} must already have
been called for that name. For composite traits (e.g. weaning weight
assembled from direct and maternal components), no \code{\link[=define_trait]{define_trait()}} call is
needed for the composite name itself.

Writes one row to \code{phenotype_meta}. If \code{residual_var} is supplied, also
writes an unconditional diagonal entry to \code{phenotype_residual_cov}. If
\code{components} is supplied, writes one row per component to
\code{phenotype_components}.
}
\examples{
\dontrun{
# Simple continuous trait
pop <- pop |>
  define_trait("ADG", target_add_var = 100) |>
  get_table("genome_meta") |>
  define_additive_effects("ADG") |>
  define_phenotype("ADG",
    trait_type   = "continuous",
    mean         = 500,
    residual_var = 120)

# Maternal composite: WW = WWD (self) + WWM (dam)
pop <- pop |>
  define_phenotype("WW",
    trait_type   = "continuous",
    mean         = 230,
    residual_var = 180,
    components   = tibble::tribble(
      ~source_trait_name, ~contributor_type,
      "WWD",              "self",
      "WWM",              "dam"
    ))
}
}
\seealso{
\code{\link[=define_trait]{define_trait()}}, \code{\link[=define_residual_cov]{define_residual_cov()}}, \code{\link[=add_phenotype]{add_phenotype()}},
\code{\link[=define_trait_simple]{define_trait_simple()}}
}
