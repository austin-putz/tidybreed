% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{migrate_schema_meta}
\alias{migrate_schema_meta}
\title{Migrate a legacy database to include _schema_meta}
\usage{
migrate_schema_meta(pop)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}
}
\value{
\code{pop}, invisibly (with \verb{_schema_meta} added to \code{pop$tables} if
it was not already present).
}
\description{
Creates the \verb{_schema_meta} table and populates it with all standard
descriptions for databases created before v0.36.0. Safe to call on a
database that already has the table (idempotent).
}
\examples{
\dontrun{
pop <- restore_pop("old_sim.duckdb")
pop <- migrate_schema_meta(pop)
schema(pop)
}
}
\seealso{
\code{\link[=schema]{schema()}}, \code{\link[=describe_table]{describe_table()}}
}
