% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_helpers.R
\name{.pivot_pheno_wide}
\alias{.pivot_pheno_wide}
\title{Pivot ind_phenotype rows wide for formula evaluation.}
\usage{
.pivot_pheno_wide(rows, ids, pheno_names)
}
\arguments{
\item{rows}{Data frame from ind_phenotype query (id_ind, phenotype_name,
pheno_value, pheno_number).}

\item{ids}{Character vector. Ordered individual IDs.}

\item{pheno_names}{Character vector. Phenotype column names to create.}
}
\value{
Data frame with one row per id in ids, columns = pheno_names.
}
\description{
Uses pheno_number = 1 by default. If, across the entire \code{rows} input, not
a single row has \code{pheno_number == 1} (e.g. first records were deleted),
falls back to the minimum \code{pheno_number} per (id_ind, phenotype_name) pair
instead. This fallback is all-or-nothing over the whole input, not decided
per phenotype or per individual.
}
\keyword{internal}
