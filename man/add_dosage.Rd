% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_dosage.R
\name{add_dosage}
\alias{add_dosage}
\title{Materialize genotype dosage values into \code{ind_genotype}}
\usage{
add_dosage(tbl, chip_name = NULL, locus_names = NULL, overwrite_dosage = FALSE)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{\link[=get_table]{get_table()}} (optionally filtered). Any
table with an \code{id_ind} column is accepted.}

\item{chip_name}{Character or \code{NULL}. Name of a chip defined via
\code{\link[=define_chip]{define_chip()}} (which writes \verb{is_<chip_name>} to \code{genome_meta}). Loci are
restricted to \verb{is_<chip_name> = TRUE}. Errors if the column is missing.}

\item{locus_names}{Character vector or \code{NULL}. Explicit loci to materialize.
Takes precedence over \code{chip_name} when both are supplied.}

\item{overwrite_dosage}{Logical. When \code{TRUE}, existing \code{ind_genotype} rows for
the candidate individuals are deleted before inserting (cache-scope reset).
When \code{FALSE} (default), rows are upserted via \verb{INSERT OR REPLACE} — dosage
is fully determined by the haplotypes, so re-running is idempotent.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Computes 0/1/2 genotype dosages (sum of alleles across an individual's
haplotype strands) from the long \code{ind_haplotype} table and writes them to the
on-demand \code{ind_genotype} cache. \code{ind_genotype} is \strong{never} auto-populated by
\code{\link[=add_founders]{add_founders()}} or \code{\link[=add_offspring]{add_offspring()}} — call \code{add_dosage()} explicitly when
you need dosage values for marker-assisted selection, allele-frequency
queries, or other downstream analysis.

Pipe a \code{tidybreed_table} (from \code{\link[=get_table]{get_table()}} and optionally \code{\link[=filter]{filter()}}) as the
first argument to select individuals. As with \code{\link[=add_tbv]{add_tbv()}}, the \strong{distinct}
\code{id_ind} values in the collected table form the candidate set, so a table with
multiple rows per individual (e.g. \code{ind_phenotype}) does not multiply work.
}
\section{\code{add_dosage()} vs. \code{\link[=add_genotypes]{add_genotypes()}}}{

These are different operations despite similar names.
\code{\link[=add_genotypes]{add_genotypes()}} marks animals as \emph{physically genotyped} on a chip by
writing a BOOLEAN \verb{has_<chip>} column to \code{ind_meta}; it touches no dosage
data. \code{add_dosage()} \strong{materializes simulated dosage values} (ground truth
from \code{ind_haplotype}) into \code{ind_genotype}.
}

\examples{
\dontrun{
# Dosage for all generation-5 candidates on the 50K chip
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 5L) |>
  add_dosage(chip_name = "50K")

# Dosage at specific QTL only
pop <- pop |>
  get_table("ind_meta") |>
  add_dosage(locus_names = c("Locus_1", "Locus_42"))

# Marker-assisted selection: homozygous-favorable at a QTL
ids <- pop |>
  get_table("ind_genotype") |>
  dplyr::filter(locus_name == "Locus_1", dosage_value == 2L) |>
  dplyr::pull(id_ind)
}
}
\seealso{
\code{\link[=add_genotypes]{add_genotypes()}}, \code{\link[=extract_genotypes]{extract_genotypes()}}, \code{\link[=define_chip]{define_chip()}}
}
