% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ebv.R
\name{add_ebv}
\alias{add_ebv}
\title{Add estimated breeding values to a tidybreed population}
\usage{
add_ebv(
  tbl,
  trait_name,
  software = NULL,
  parent_avg = FALSE,
  model = "model_1",
  overwrite_trait = FALSE,
  delete_all = FALSE,
  n_gen_pedigree = 2L,
  chip_name = NULL,
  run_dir = ".",
  eval_id = NULL,
  estimate_var = FALSE,
  update_covars = FALSE,
  phenotype = NULL,
  ...
)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{\link[=get_table]{get_table()}} (optionally filtered).
Must contain an \code{id_ind} column.}

\item{trait_name}{Character vector of trait name(s) to evaluate.}

\item{software}{Character or \code{NULL} (default). Set to \code{"blupf90"} to run
the BLUPF90 suite (currently the only supported value). Exactly one of
\code{software = "blupf90"} or \code{parent_avg = TRUE} must be supplied — leaving
both at their defaults, or setting both, is an error.}

\item{parent_avg}{Logical. If \code{TRUE}, compute EBVs as the average of
parent EBVs from \code{ind_ebv} (for the given \code{model}). Cannot be combined
with \code{software}; see \code{software} above. Default \code{FALSE}.}

\item{model}{Character. Label stored in \code{ind_ebv$model}. Must be a valid
SQL identifier.  Default \code{"model_1"}.}

\item{overwrite_trait}{Logical. If \code{TRUE}, all existing \code{ind_ebv} rows for
the traits being added are deleted before inserting; new rows receive
\code{eval_number = 1}. Default \code{FALSE}.}

\item{delete_all}{Logical. If \code{TRUE}, \strong{all} rows in \code{ind_ebv} are deleted
before inserting; new rows receive \code{eval_number = 1}. Takes precedence
over \code{overwrite_trait}. Default \code{FALSE}.}

\item{n_gen_pedigree}{Integer. Generations to trace back from the filtered
candidates when building the pedigree file. Default \code{2}. Used only in
\code{software = "blupf90"} mode; ignored under \code{parent_avg = TRUE}.}

\item{chip_name}{Character or \code{NULL}. Name of a SNP chip defined via
\code{\link[=define_chip]{define_chip()}} (e.g. \code{"HD"}). When provided, genotype file is written
and single-step GBLUP (ssGBLUP) is requested via \code{SNP_FILE} in
\code{renum.par}. Animals must have \verb{has_\{chip_name\} = TRUE} in \code{ind_meta}
(validated up front regardless of mode). Used only in
\code{software = "blupf90"} mode; silently unused under \code{parent_avg = TRUE}.}

\item{run_dir}{Character. Base directory for evaluation sub-folders.
Default \code{"."} (working directory). Ignored when \code{pop$run_dirs} is
non-empty (i.e. when \code{\link[=open_pop]{open_pop()}} was called with a \code{tools} argument) —
the managed output folder structure is used instead. Applies only in
\code{software = "blupf90"} mode.}

\item{eval_id}{Character or \code{NULL}. Sub-folder name suffix appended to
\code{"eval_"}. Auto-generated from \code{model} + timestamp when \code{NULL}. Ignored
in managed output mode (see \code{run_dir} above); the run directory name
generated by \code{.create_run_dir()} is used as the identifier. Applies only
in \code{software = "blupf90"} mode.}

\item{estimate_var}{Logical. If \code{TRUE}, request variance component
estimation (\verb{OPTION method VCE}) instead of BLUP-only. Default \code{FALSE}.
Applies only in \code{software = "blupf90"} mode.}

\item{update_covars}{Logical. If \code{estimate_var = TRUE}, attempt to write
estimated variance components back to \code{trait_var_comp}. Default \code{FALSE}.
\strong{Not yet implemented}: the current writeback step
(\code{update_covars_from_blupf90()}) is a stub that only prints a message
pointing you to \code{blupf90.out} and \code{\link[=define_effect_cov_matrix]{define_effect_cov_matrix()}} for a
manual update — no rows are written automatically yet. Applies only in
\code{software = "blupf90"} mode.}

\item{phenotype}{Optional \code{tidybreed_table} from
\code{get_table("ind_phenotype") |> filter(...)}. When supplied, only phenotype
records matching the filter are included in the BLUPF90 data file. The
pedigree is unaffected — all traced ancestors remain in the pedigree file
regardless of this filter. Ignored (with a warning) in \code{parent_avg} mode.
Applies only in \code{software = "blupf90"} mode.}

\item{...}{Optional extra columns written to \code{ind_ebv} (scalars only;
broadcast to all records). Supply per-record vectors with
\code{\link[=mutate_table]{mutate_table()}} after the call.}
}
\value{
The modified \code{tidybreed_pop} (invisibly). EBVs are appended to
\code{ind_ebv} with an auto-incrementing \code{eval_number} per trait. Use
\code{overwrite_trait = TRUE} or \code{delete_all = TRUE} to clear previous records
instead of accumulating them.
}
\description{
Computes EBVs using one of two modes:
\itemize{
\item \code{software = "blupf90"} — runs the BLUPF90 suite (\code{renumf90} + \verb{blupf90+}).
Both binaries must be on PATH. Writes all input/output files to a
timestamped subfolder of \code{run_dir} for inspection and reproducibility.
\item \code{parent_avg = TRUE} — simple average of parent EBVs already stored in
\code{ind_ebv} for the given \code{model}. Returns \code{NA} for any animal whose
parents do not both have EBVs.
}

The first argument is a \code{tidybreed_table} (from \code{\link[=get_table]{get_table()}} and
optionally \code{\link[dplyr:filter]{dplyr::filter()}}). The filter controls which animals' \strong{phenotypic
records} are included in the evaluation; ancestors are traced back
automatically for the pedigree via \code{n_gen_pedigree}.
}
\examples{
\dontrun{
# Parent average (parents must already have EBVs in ind_ebv for model
# "pa_v1" -- see the versioning example below for how eval_number accrues)

# Model / eval_number versioning: each call for the same `model` label
# appends a new eval_number rather than overwriting (ind_ebv's logical key
# is id_ind x trait_name x model x eval_number). Re-running as new data
# arrives builds up a history of evaluations you can compare over time;
# `overwrite_trait = TRUE` instead clears prior rows for the trait(s) and
# restarts eval_number at 1 for that model.
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 2L) |>
  add_ebv("ADG", parent_avg = TRUE, model = "pa_v1")   # eval_number = 1
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 3L) |>
  add_ebv("ADG", parent_avg = TRUE, model = "pa_v1")   # eval_number = 2
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 2L) |>
  add_ebv("ADG", parent_avg = TRUE, model = "pa_v1", overwrite_trait = TRUE) # back to 1

# BLUPF90 — renumf90 and blupf90+ must be on PATH
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen >= 3L) |>
  add_ebv(c("ADG", "BW"),
          software = "blupf90",
          model    = "blup_v1",
          run_dir  = "./eval_runs",
          n_gen_pedigree = 2)

# BLUPF90 with a phenotype filter (e.g. excluding a custom-added date
# column's future/incomplete records) -- `pheno_date` here is a user-added
# column (e.g. via add_phenotype(..., pheno_date = Sys.Date())), not a
# built-in ind_phenotype column
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen >= 3L) |>
  add_ebv(c("ADG", "BF"),
          software  = "blupf90",
          model     = "blup_v1",
          phenotype = pop |>
                        get_table("ind_phenotype") |>
                        dplyr::filter(pheno_date < Sys.Date()))
}
}
\seealso{
\code{\link[=get_table]{get_table()}}, \code{\link[=add_phenotype]{add_phenotype()}}, \code{\link[=define_chip]{define_chip()}},
\code{\link[=define_effect_cov_matrix]{define_effect_cov_matrix()}}
}
