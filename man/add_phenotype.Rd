% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_phenotype.R
\name{add_phenotype}
\alias{add_phenotype}
\title{Generate phenotype records for a subset of individuals}
\usage{
add_phenotype(
  tbl,
  phenotype_name = NULL,
  user_residual = NULL,
  user_values = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{\link[=get_table]{get_table()}}, optionally piped through
\code{\link[dplyr:filter]{dplyr::filter()}}. Any table with an \code{id_ind} column is accepted; the
individuals acted on are the distinct \code{id_ind} values present in the
(filtered) table. An unfiltered \code{ind_meta} selects every individual; an
unfiltered \code{ind_ebv}, \code{ind_index}, \code{ind_genotype}, ... selects only the
individuals that have rows there. A table without \code{id_ind} is an error.}

\item{phenotype_name}{Character vector of phenotype name(s). When \code{NULL}
(default), all phenotypes in \code{phenotype_meta} are used in
\code{id_phenotype_meta} order.}

\item{user_residual}{Optional residuals to use instead of drawing them
(the mean, covariate and TBV contributions are still computed and
added). When exactly one phenotype in the call is generated from the
model, a plain numeric vector matched \strong{by position} to that
phenotype's planned records — sorted \code{id_ind} order after sex
expression, the repeatable guard and any exclusion, so its length must
equal the planned record count, which may be smaller than the filtered
\code{tbl}. Otherwise a named list keyed by \code{phenotype_name} that may name
\strong{any subset} of the model-generated phenotypes, each element following
the same positional rule; the phenotypes not named are drawn conditional
on the supplied values. Supplied residuals are stored in
\code{residual_value} like drawn ones and condition later calls; a phenotype
whose residuals are all supplied needs no residual variance declared.
A value outside the support of a singular covariance (e.g. non-zero
for a zero-variance phenotype) is an error. Named (per-\code{id_ind}) vectors are
\strong{not} supported here; cannot be combined with \code{user_values}.}

\item{user_values}{Optional override for the full phenotype value —
skips the model entirely (mean, covariates, and residual are not
evaluated), though TBVs are still computed and stored in \code{ind_tbv}. For a
single \code{phenotype_name}: a plain numeric vector matching the planned
records by position (sorted \code{id_ind} order, after sex expression and the
repeatable guard), or a named numeric vector (e.g.
\code{c(id_1 = 555, id_2 = 560)}) to match by \code{id_ind} regardless of order —
every name must be an individual in that planned set, each once, and
only the named individuals receive a record. For multiple phenotypes: a
named list keyed by \code{phenotype_name}, each element following the same
(positional-or-named) rule.}

\item{seed}{Optional integer for reproducibility.}

\item{...}{Optional scalar extra columns written to \code{ind_phenotype}
(broadcast to all records). Supply per-record vectors with
\code{\link[=mutate_table]{mutate_table()}} after the call.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Simulates phenotype values for one or more phenotypes and writes them to
\code{ind_phenotype}. Also computes and stores the underlying true breeding
value (TBV) per individual per trait in \code{ind_tbv}.

\strong{Model} (per phenotype, on the liability / continuous scale):
\preformatted{
  y_i = mean + sum(fixed_shifts) + sum(random_shifts) + TBV_i + e_i
}
\itemize{
\item \code{mean} comes from \code{phenotype_meta.mean}.
\item Fixed shifts come from the \code{fixed_class} / \code{fixed_cov} rows of
\code{phenotype_effects}.
\item Random shifts come from its \code{random} rows (see \code{\link[=define_effect_random]{define_effect_random()}}):
one draw per distinct level of the effect's \code{source_column}, realized
the first time any record touches the level and stored in
\code{phenotype_random_effects}, then reused by every later record with that
level — in this call or any later one. A level is persistent: a pen
that is re-realized per batch is a different level (\code{pen_batch}), not a
different feature. When the effect is correlated across phenotypes
(\code{\link[=define_effect_cov_matrix]{define_effect_cov_matrix()}} with the effect's name), a level's draw
for one phenotype is conditional on the draws it already has stored for
the block's other phenotypes — \code{add_phenotype("ADG")} today and
\code{add_phenotype("BF")} next season gives pen \code{P1} a \verb{(ADG, BF)} pair with
the declared covariance, whichever came first. A record whose level is
\code{NULL} gets no draw and a shift of \code{0}.
\item For \strong{simple} phenotypes (\code{phenotype_name == trait_name}), \code{TBV_i} is the
standard additive TBV from \code{genome_effects} (computed via \code{\link[=add_tbv]{add_tbv()}},
which this function calls internally for every source trait it needs).
\item For \strong{composite} phenotypes (rows in \code{phenotype_components}, written by
\code{define_phenotype(..., components = ...)}), \code{TBV_i} is the weighted sum
of contributor TBVs (self, dam, sire, or group) — see
\code{.assemble_composite_tbv()}.
\item For \strong{\code{formula_tbv}} composite phenotypes (\code{phenotype_meta.formula_tbv}
set, written by \code{define_phenotype(..., formula_tbv = ...)}), \code{TBV_i} is
evaluated from a small DSL expression referencing self/dam/sire/group
TBVs instead of a \code{phenotype_components} data frame.
\item \code{e_i} is the residual, drawn from the phenotype's residual covariance
block in \code{phenotype_var_comp} (see \code{\link[=define_residual_cov]{define_residual_cov()}}). Within a
block, residuals are \strong{correlated across phenotypes and sequential in
time}: each record is drawn from the block's multivariate normal
conditional on every residual the same individual has already realized
for the other phenotypes of the block at the same \code{pheno_number} —
whether those were drawn in this call, in an earlier call, or supplied
through \code{user_residual}. So \code{add_phenotype("A")} today and
\code{add_phenotype("B")} after culling gives the survivors' \code{B} residual
the stored correlation with their \code{A} residual, with no \code{B} record for
the culled. Heterogeneous residual variance (strata by
\code{condition_column}) is applied per record: each record draws from the
stratum its condition value selects, falling back to the unconditional
\code{R} (with \code{residual_condition_level} stored as \code{NULL}) when the value
is \code{NULL} or matches no stratum, and erroring if there is no
unconditional stratum to fall back on. A stored residual drawn under a
different stratum than the current record resolves to is an error, or
is dropped from the conditioning set with a warning when the block's
phenotypes have \code{condition_change_action = "independent"}. The realized
residual and its stratum are written to \code{ind_phenotype.residual_value}
/ \code{residual_condition_level} (liability scale; \code{NULL} for \code{user_values}
and \code{derived_formula} records). \code{pheno_number} pairs records
ordinally, not by simulated time; repeated records of the \emph{same}
phenotype have independent residuals (use a permanent-environment
random effect for within-animal covariance).
}

\strong{\code{derived_formula} phenotypes are the one exception to the model above.}
When \code{phenotype_meta.type == "derived_formula"} (\code{phenotype_meta.formula}
set), the phenotype value is computed directly as an arithmetic expression
over other individuals' already-written \code{ind_phenotype} records — there is
no TBV, no mean/fixed/random contribution, and no residual draw for that
phenotype. When a call mixes \code{derived_formula} phenotypes with others that
feed them, the phenotypes are topologically sorted first so dependencies
are written before the formulas that consume them.

\strong{Subset selection}: pipe a \code{tidybreed_table} (from \code{\link[=get_table]{get_table()}} and
optionally \code{\link[dplyr:filter]{dplyr::filter()}}) as the first argument.

\strong{How a call runs.} Every record is planned first — sex expression, the
repeatable guard, fixed-effect skips, missing-component exclusions and
\code{pheno_number} are all decided before a single random number is drawn —
then every draw is made in memory, then everything is written in one
transaction. An individual that ends up without a record therefore never
consumes RNG, and seeded output does not depend on physical row order:
records are planned in \code{id_ind} order within each phenotype. See
\code{?add_phenotype_stages}.

\strong{If a call fails}, nothing is written: \code{ind_phenotype} and
\code{phenotype_random_effects} are exactly as they were, whether the error
came from validation, from sampling (a missing variance, a residual
stratum change under \code{condition_change_action = "error"}) or from the
write itself — down to the schema, so a new column named in \code{...} is
rolled back with the rows it was added for. The random-number stream is \strong{not} rewound: \code{.Random.seed}
stays advanced by the draws made before the error, as after any other
failed R call, so re-running the call draws different values. Pass \code{seed}
(or call \code{set.seed()}) again if the retry must reproduce the failed call.
The TBVs the call materialized through \code{\link[=add_tbv]{add_tbv()}} remain; they do not
depend on the RNG, and the retry rewrites them.

\strong{Escape hatches}:
\itemize{
\item \code{user_values}: skip model computation and write these values as phenotype
records for the subset.
\item \code{user_residual}: supply the residuals of some or all phenotypes instead
of drawing them; the rest are drawn conditional on the supplied values.
}
}
\examples{
\dontrun{
# All individuals — all phenotypes
pop <- pop |> get_table("ind_meta") |> add_phenotype()

# Named phenotype, filtered subset
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(sex == "F", gen == 1L) |>
  add_phenotype("ADG")

# Any table with id_ind chooses the individuals. Marker-assisted
# pre-selection: only carriers of two copies at Locus_10 (run add_dosage()
# first, since ind_genotype is an on-demand cache)
pop <- pop |>
  get_table("ind_genotype") |>
  dplyr::filter(locus_name == "Locus_10", dosage_value == 2L) |>
  add_phenotype("ADG")

# EBV-based: only animals above an EBV threshold in the latest evaluation
pop <- pop |>
  get_table("ind_ebv") |>
  dplyr::filter(trait_name == "ADG", eval_number == 3L, ebv_value > 0.5) |>
  add_phenotype("ADG")

# Unfiltered ind_ebv means "every animal that has an EBV", not everyone
pop <- pop |> get_table("ind_ebv") |> add_phenotype("ADG")

# Composite (maternal) phenotype: WW = direct (self) + maternal (dam) TBV,
# registered once via define_phenotype(components = ...)
pop <- pop |>
  define_phenotype("WW", type = "continuous", mean = 230, residual_var = 180,
    components = tibble::tribble(
      ~source_trait_name, ~contributor_type,
      "WWD",              "self",
      "WWM",              "dam"
    ))
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 1L) |>
  add_phenotype("WW")

# Escape hatch: supply phenotype values directly (skips the model, but
# still computes and stores TBVs); named vector matches by id_ind
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 0L, sex == "M") |>
  add_phenotype("ADG", user_values = c(A_1 = 555, A_2 = 560))
}
}
\seealso{
\code{\link[=define_phenotype]{define_phenotype()}}, \code{\link[=define_trait]{define_trait()}}, \code{\link[=define_additive_effects]{define_additive_effects()}},
\code{\link[=define_residual_cov]{define_residual_cov()}}, \code{\link[=define_effect_cov_matrix]{define_effect_cov_matrix()}},
\code{\link[=define_effect_fixed_class]{define_effect_fixed_class()}}, \code{\link[=define_effect_fixed_cov]{define_effect_fixed_cov()}},
\code{\link[=define_effect_random]{define_effect_random()}}, \code{\link[=add_tbv]{add_tbv()}}
}
