% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_trait.R
\name{add_trait}
\alias{add_trait}
\title{Add a trait definition to the population}
\usage{
add_trait(
  pop,
  trait_name,
  description = NA_character_,
  units = NA_character_,
  trait_type = c("continuous", "count", "binary", "categorical"),
  repeatable = FALSE,
  recorded_on = c("self", "dam", "sire", "offspring_mean"),
  expressed_sex = c("both", "M", "F"),
  expressed_parent = c("both", "parent_1", "parent_2"),
  target_add_mean = 0,
  target_add_var = 1,
  residual_var = 1,
  min_value = NA_real_,
  max_value = NA_real_,
  prevalence = NA_real_,
  thresholds = NA_real_,
  index_weight = 0,
  economic_value = 0,
  overwrite = FALSE
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{trait_name}{Character. Trait name; used as the primary key and as the
suffix for \verb{is_QTL_\{trait_name\}} and \verb{add_\{trait_name\}} columns in \code{genome_meta}.
Must be a valid SQL identifier.}

\item{description}{Character. Free-text description of the trait.}

\item{units}{Character. Measurement units, e.g. \code{"kg"}, \code{"count"}.}

\item{trait_type}{Character. One of \code{"continuous"}, \code{"count"}, \code{"binary"},
\code{"categorical"}.}

\item{repeatable}{Logical. Whether an individual can have multiple records
for this trait (e.g. test-day yield).}

\item{recorded_on}{Character. Which animal records the trait:
\code{"self"}, \code{"dam"}, \code{"sire"}, or \code{"offspring_mean"}.}

\item{expressed_sex}{Character. Sex that expresses the trait: \code{"both"},
\code{"M"}, or \code{"F"}.}

\item{expressed_parent}{Character. Parent-of-origin expression:
\code{"both"}, \code{"parent_1"} (paternal), \code{"parent_2"} (maternal). Imprinted
traits use only the haplotype from the specified parent.}

\item{target_add_mean}{Numeric. Target mean additive genetic value for the
base population. Used as the intercept in the phenotype model. Defaults to
0 so that \code{E[TBV] = 0} when TBV is centered on base allele frequencies.}

\item{target_add_var}{Numeric. Target additive-genetic variance used by
\code{\link[=set_qtl_effects]{set_qtl_effects()}} to rescale sampled effects.}

\item{residual_var}{Numeric. Residual variance used by \code{\link[=add_phenotype]{add_phenotype()}}.}

\item{min_value, max_value}{Numeric. Clipping bounds for count traits. \code{NA}
means no limit.}

\item{prevalence}{Numeric between 0 and 1. For binary traits, the fraction
expected to be affected; determines the liability threshold.}

\item{thresholds}{Numeric vector. For categorical traits, liability
cutpoints separating ordered levels. Stored as a comma-separated string.}

\item{index_weight}{Numeric. Weight in a downstream selection index.}

\item{economic_value}{Numeric. Economic value per unit of the trait.}

\item{overwrite}{Logical. If \code{TRUE} and a trait with the same name already
exists, replace its \code{trait_meta} row. Associated rows in \code{trait_effects}
and \code{trait_residual_cov} are cleared for that trait.}
}
\value{
The modified \code{tidybreed_pop} (invisibly). Assign the result back.
}
\description{
Creates one row in the \code{trait_meta} table describing a trait's genetic and
phenotypic architecture. The row captures target variance components,
trait type (continuous / count / binary / categorical), expression rules
(sex-limited, parent-of-origin), and index / economic weights.

\code{add_trait()} does not touch the genome or sample QTL. Those are separate
steps handled by \code{\link[=define_qtl]{define_qtl()}} and \code{\link[=set_qtl_effects]{set_qtl_effects()}}. For the common
one-off case, use \code{\link[=add_trait_simple]{add_trait_simple()}} which chains all three together.

On first call, creates the six trait-layer tables (\code{trait_meta},
\code{trait_effects}, \code{trait_residual_cov}, \code{ind_phenotype}, \code{ind_tbv},
\code{ind_ebv}).
}
\examples{
\dontrun{
pop <- pop |>
  add_trait(
    trait_name     = "ADG",
    trait_type     = "continuous",
    units          = "g/day",
    target_add_mean = 0,
    target_add_var = 0.25,
    residual_var   = 0.75
  )
}
}
\seealso{
\code{\link[=define_qtl]{define_qtl()}}, \code{\link[=set_qtl_effects]{set_qtl_effects()}}, \code{\link[=add_trait_covariate]{add_trait_covariate()}},
\code{\link[=add_phenotype]{add_phenotype()}}, \code{\link[=add_trait_simple]{add_trait_simple()}}
}
