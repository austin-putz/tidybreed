% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_group_named.R
\name{mutate_group_named}
\alias{mutate_group_named}
\title{Assign named group labels to filtered rows by count or proportion}
\usage{
mutate_group_named(
  tbl,
  col_name,
  group_names,
  counts = NULL,
  proportions = NULL,
  prop_method = "exact",
  include_leftover = FALSE,
  underfill_action = "error",
  seed = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{get_table()} (optionally filtered).}

\item{col_name}{Character scalar. Name of the group column to create or
update. Must be a valid SQL identifier and not a reserved column.}

\item{group_names}{Character vector. Group labels. Must be the same length
as \code{counts} or \code{proportions}.}

\item{counts}{Integer vector of animals per group. Mutually exclusive with
\code{proportions}.}

\item{proportions}{Numeric vector of relative group fractions. Mutually
exclusive with \code{counts}.}

\item{prop_method}{\code{"exact"} (default) uses the largest-remainder method for
deterministic integer rounding; \code{"random"} draws each label independently.}

\item{include_leftover}{Logical. When \code{sum(counts) < n_filtered} (or when
proportions sum to < n due to rounding), add the unassigned rows to the
last group. Default \code{FALSE} leaves them \code{NULL}.}

\item{underfill_action}{\code{"error"} (default), \code{"proportional"}, or
\code{"sequential"}. Only applies when \code{counts} is supplied and
\code{sum(counts) > n_filtered}.}

\item{seed}{Integer or \code{NULL}. If supplied, sets the RNG seed before
shuffling. Validation happens before the seed is set.}

\item{overwrite}{Logical. If \code{FALSE} (default), error when any filtered row
already has a non-\code{NULL} value in \code{col_name}.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages.}
}
\value{
The modified \code{tidybreed_pop} object (invisibly).
}
\description{
Randomly assigns each filtered row to one of the supplied \code{group_names}.
Group membership is determined either by explicit \code{counts} (how many animals
per group) or by \code{proportions} (relative fractions). The column is always
\code{VARCHAR}. Useful for farm assignment, treatment groups, line splitting, and
any situation where groups have user-defined string labels.

Pipe \code{get_table("ind_meta")} — optionally through \code{dplyr::filter()} — into
this function. Filtered primary keys are sorted ascending before shuffling,
giving a stable, reproducible starting point.
}
\section{Counts vs proportions}{

Exactly one of \code{counts} or \code{proportions} must be supplied.
\itemize{
\item \strong{\code{counts}}: integer vector, same length as \code{group_names}. When
\code{sum(counts) > n_filtered}, \code{underfill_action} decides what happens.
When \code{sum(counts) < n_filtered}, remaining rows are left \code{NULL} unless
\code{include_leftover = TRUE} (which adds them to the last group).
\item \strong{\code{proportions}}: numeric vector, same length as \code{group_names}. Values
must be positive and are normalized to sum to 1 within
\code{sqrt(.Machine$double.eps)} tolerance. With \code{prop_method = "exact"} (default),
the largest-remainder method converts proportions to integer counts,
tie-breaking left-to-right. With \code{prop_method = "random"}, each animal is
independently drawn from the distribution.
}
}

\section{Underfill behavior (counts mode only)}{

When \code{sum(counts) > n_filtered} and \code{underfill_action}:
\itemize{
\item \code{"error"} (default): stop before sampling.
\item \code{"proportional"}: rescale counts to fit \code{n_filtered} using the
largest-remainder method.
\item \code{"sequential"}: fill groups in order until animals run out; later groups
may be partially or fully empty.
}
}

\examples{
\dontrun{
# Assign females to two farms by count
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(sex == "F") |>
  mutate_group_named(
    col_name    = "farm",
    group_names = c("farm_A", "farm_B"),
    counts      = c(60L, 40L)
  )

# Assign all individuals by proportion (60 / 40 split)
pop <- pop |>
  get_table("ind_meta") |>
  mutate_group_named(
    col_name     = "farm",
    group_names  = c("farm_A", "farm_B"),
    proportions  = c(0.6, 0.4)
  )

# If available animals are fewer than requested counts, rescale proportionally
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 1L) |>
  mutate_group_named(
    col_name         = "trt",
    group_names      = c("control", "high", "low"),
    counts           = c(50L, 50L, 50L),
    underfill_action = "proportional"
  )
}
}
\seealso{
\code{\link[=mutate_group_seq]{mutate_group_seq()}}, \code{\link[=mutate_group_concatenate]{mutate_group_concatenate()}}, \code{\link[=mutate_table]{mutate_table()}}
}
