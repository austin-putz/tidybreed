% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_genome_effects.R
\name{define_genome_effects}
\alias{define_genome_effects}
\title{Define genome effects as terms over one or more loci}
\usage{
define_genome_effects(
  pop,
  trait_name,
  terms,
  effect_owner = "custom",
  mode = c("append", "replace_scope", "replace_owner", "replace_trait"),
  origin = NULL,
  base_tbl = NULL,
  require_complete = FALSE,
  allow_reserved_owner = FALSE
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop}.}

\item{trait_name}{Character scalar. Must exist in \code{trait_meta}.}

\item{terms}{Long data frame; see \strong{The \code{terms} data frame}.}

\item{effect_owner}{Character scalar naming the writer that owns these rows,
\strong{for replacement only}. Owners always sum and are never selected between,
because \code{effect_owner} is part of the fallback-family signature. Default
\code{"custom"}.}

\item{mode}{One of \code{"append"}, \code{"replace_scope"}, \code{"replace_owner"},
\code{"replace_trait"}.}

\item{origin}{Scope; see \strong{Scope (\code{origin})}.}

\item{base_tbl}{Optional \code{tidybreed_table} from \code{\link[=get_table]{get_table()}} (optionally
filtered) selecting the allele copies whose frequencies fill
\code{center_value} on any \code{additive} or \code{dominance} member that has none:
\code{founder_haplotypes}, \code{ind_haplotype}, or any table with an \code{id_ind}
column, from the same \code{pop} — see \code{\link[=extract_allele_freq]{extract_allele_freq()}}. An explicit
\code{center_value} is never overwritten; \code{indicator} members are never
touched; the base is queried only if some centre is actually missing.
\code{NULL} (default) fills nothing, and a missing centre is an error. The
fill is Cockerham \code{p} only — functional coding writes \code{0.5} explicitly
(\code{\link[=ad_terms]{ad_terms()}} does). One \code{base_tbl} gives one \code{p} per locus per call, so
line-scoped surfaces are written one line at a time with
\code{mode = "append"}.}

\item{require_complete}{Logical. When \code{TRUE}, an indicator surface must name
every reachable \verb{(copy_count, dosage)} state on every member — including
\code{copy_count_value = 0} where a chromosome can be absent. Default \code{FALSE}
(sparse: a cell you do not write contributes zero).}

\item{allow_reserved_owner}{Logical. Permit writing under a package-reserved
\code{effect_owner}. Default \code{FALSE}; the package's own generator
(\code{\link[=define_additive_effects]{define_additive_effects()}}) writes under its reserved owner through the
same engine.}
}
\value{
The \code{tidybreed_pop}, invisibly.
}
\description{
Writes genome effects in the term / member / origin form: one coefficient
(\code{genome_effects}) over one or more loci (\code{genome_effect_members}), each
locus optionally scoped to allele copies of a given line and/or parent of
origin (\code{genome_effect_member_origins}).

\code{terms} is a \strong{long data frame, one row per (term x locus)} — the same shape
as every table \code{\link[=get_table]{get_table()}} returns. Scope is supplied separately in
\code{origin} so the common case stays flat.

\code{define_genome_effects()} writes any effect you supply; \verb{define_*_effects()}
functions such as \code{\link[=define_additive_effects]{define_additive_effects()}} sample effects of one shape
and write them through the same path.
}
\section{The \code{terms} data frame}{
\tabular{lll}{
   Column \tab Required \tab Meaning \cr
   \code{term_id} \tab yes, unless one term \tab Groups rows into one term. \strong{User-facing only} — never stored; the writer replaces it with an \code{id_genome_effect}. Any atomic type \cr
   \code{genome_value} \tab yes \tab The term's coefficient. Constant within a \code{term_id} \cr
   \code{effect_name} \tab no \tab Per-term label. Constant within a \code{term_id}; no mathematical meaning \cr
   \code{locus_name} \tab yes \tab Resolved to \code{locus_id}; each locus at most once per term \cr
   \code{contrast_name} \tab yes \tab \code{"additive"}, \code{"dominance"} or \code{"indicator"} \cr
   \code{center_value} \tab non-indicator \tab \code{p} for Cockerham coding, \code{0.5} for functional \cr
   \code{copy_count_value}, \code{dosage_value} \tab indicator \tab The local genotype state. \code{copy_count_value} is inferred at ordinary diploid-autosomal loci \cr
}


A single-term call may omit \code{term_id} entirely.
}

\section{Scope (\code{origin})}{

\itemize{
\item \code{NULL} (default) — the common scope: no origin rows, matches every allele
copy.
\item A \strong{named scalar list}, e.g. \code{list(line_name = "Duroc", parent_origin = 1)}
— one scope applied to every member of every term. Accepted names are
\code{line_match_type}, \code{line_name}, \code{parent_origin} and \code{copy_count}.
\item A \strong{data frame} with columns \code{term_id}, \code{locus_name}, \code{line_match_type},
\code{line_name}, \code{parent_origin}, \code{copy_count} — per-member scopes, needed for
the exact multisets a \code{dominance} or \code{indicator} member takes. Keyed by
\code{locus_name}, so you never touch canonical slot order.
}

An \code{additive} member takes at most one origin row; a genotype member takes an
exact multiset whose \code{copy_count}s sum to the state's copy count (2 for
\code{dominance}, \code{copy_count_value} for \code{indicator}). A scalar list therefore
scopes \code{additive} members but is usually not enough for a genotype member —
the validator says so, naming the locus.
}

\section{Replacement modes}{

\itemize{
\item \code{"append"} — insert; a term duplicating an existing family + scope identity
is rejected.
\item \code{"replace_scope"} — delete only the variants in \verb{(trait_name, effect_owner)}
whose origin predicate \strong{equals} the supplied scope, then insert. This is
what \code{\link[=define_additive_effects]{define_additive_effects()}} uses, so successive common / line-A /
line-B calls each replace only their own variant. Requires \code{origin} to be
\code{NULL} or a scalar list (a per-member \code{origin} data frame has no single
scope to key on).
\item \code{"replace_owner"} — replace everything under \verb{(trait_name, effect_owner)}.
\item \code{"replace_trait"} — clear every owner's terms for the trait. Never implied.
}
}

\examples{
\dontrun{
# One dominance term, Cockerham coding at p = 0.3
pop <- pop |> define_genome_effects(
  trait_name = "ADG",
  terms = data.frame(locus_name    = "Locus_10",
                     contrast_name = "dominance",
                     center_value  = 0.3,
                     genome_value  = 0.8)
)

# A hand-entered 3x3 A x A surface: nine cells, nine terms, two members each
cells <- expand.grid(g1 = 0:2, g2 = 0:2)
cells$value <- c(0, 0, 0,  0, 1.4, 2.1,  0, 2.1, 3.6)
surface <- rbind(
  data.frame(term_id = seq_len(nrow(cells)), locus_name = "Locus_10",
             contrast_name = "indicator", dosage_value = cells$g1,
             genome_value  = cells$value),
  data.frame(term_id = seq_len(nrow(cells)), locus_name = "Locus_44",
             contrast_name = "indicator", dosage_value = cells$g2,
             genome_value  = cells$value)
)
pop <- pop |> define_genome_effects("ADG", surface[surface$genome_value != 0, ],
                                    effect_owner = "epistasis_AxA")

# Reciprocal dominance: the F1 value depends on which parent gave which line
pop <- pop |> define_genome_effects(
  "ADG",
  terms = data.frame(term_id = 1L, locus_name = "Locus_10",
                     contrast_name = "dominance",
                     center_value = 0.3, genome_value = 1.2),
  origin = data.frame(term_id = 1L, locus_name = "Locus_10",
                      line_match_type = "exact",
                      line_name     = c("Duroc", "Landrace"),
                      parent_origin = c(1L, 2L),
                      copy_count    = c(1L, 1L)),
  effect_owner = "reciprocal"
)

# Let the writer fill Cockerham p from a base population: leave
# center_value out and pass base_tbl (see extract_allele_freq()).
pop <- pop |> define_genome_effects(
  "ADG",
  data.frame(locus_name = "Locus_10", contrast_name = "dominance",
             genome_value = 0.8),
  base_tbl = get_table(pop, "founder_haplotypes") |>
    dplyr::filter(line_name == "A")
)
}
}
\seealso{
\code{\link[=ad_terms]{ad_terms()}} and \code{\link[=genotype_terms]{genotype_terms()}} build \code{terms};
\code{\link[=define_additive_effects]{define_additive_effects()}} for generated additive QTL effects.
}
