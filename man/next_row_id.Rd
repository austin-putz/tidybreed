% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql_utils.R
\name{next_row_id}
\alias{next_row_id}
\title{Get the next ID for a BIGINT primary-key sequence (overflow-safe).}
\usage{
next_row_id(conn, table, id_col)
}
\arguments{
\item{conn}{A DBI connection.}

\item{table}{Character. Table name.}

\item{id_col}{Character. BIGINT PK column name.}
}
\value{
Numeric (double) scalar.
}
\description{
Like \code{\link[=next_int_id]{next_int_id()}} but returns a \strong{double} rather than an \code{integer}, so it
is safe for \code{BIGINT} PK columns whose values can exceed \code{.Machine$integer.max}
(\code{2^31 - 1}). \code{as.integer()} would return \code{NA} past that ceiling; a double is
exact to \code{2^53}. Used for \code{ind_crossover.id_crossover}, which accumulates far
more rows than any \code{INTEGER}-keyed table. DuckDB already returns \code{BIGINT}
\code{MAX()} as an R double, so no precision is lost on the read.
}
\details{
For multi-row inserts, pre-generate a range:
\code{seq.int(next_row_id(conn, tbl, col), length.out = n)}
}
\keyword{internal}
