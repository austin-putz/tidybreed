% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open_pop.R
\name{open_pop}
\alias{open_pop}
\title{Open a new breeding population}
\usage{
open_pop(
  pop_name = getOption("tidybreed.pop_name", "sim"),
  base_dir = getOption("tidybreed.base_dir", getwd()),
  output_dir = getOption("tidybreed.output", "tidybreed_output"),
  scenario_dir = getOption("tidybreed.scenario", NULL),
  tools = getOption("tidybreed.tools", NULL),
  db_name = getOption("tidybreed.db_name", "sim.duckdb"),
  clean = TRUE
)
}
\arguments{
\item{pop_name}{Character scalar. Optional label stored on the pop object.
Default: \code{getOption("tidybreed.pop_name", "sim")}.}

\item{base_dir}{Character scalar. Root location (layer 1). Default:
\code{getOption("tidybreed.base_dir", getwd())}.}

\item{output_dir}{Character scalar. Output folder name (layer 2). Default:
\code{getOption("tidybreed.output", "tidybreed_output")}.}

\item{scenario_dir}{Character scalar or \code{NULL}. Scenario name (layer 3).
When \code{NULL} (default), a \code{YYYYMMDD_HHMMSS} timestamp folder is
auto-generated so runs are always isolated.
Default: \code{getOption("tidybreed.scenario", NULL)}.}

\item{tools}{Character vector or \code{NULL}. Tool subdirectory names to create
at layer 4 (e.g. \code{c("blupf90", "plink")}). These become the keys of
\code{pop$run_dirs}. Default: \code{getOption("tidybreed.tools", NULL)}. Ignored
(no tool dirs are created; \code{pop$run_dirs} is \code{character(0)}) whenever
\code{db_name} is \code{":memory:"} or an absolute/UNC path — see \code{db_name} below.}

\item{db_name}{Character scalar. DuckDB filename placed inside the layer-3
folder. Use \code{":memory:"} for an in-memory database, or supply an
absolute/UNC path to use that exact file location directly — both skip
all layer-2/3/4 folder creation entirely (including any \code{tools} dirs;
\code{pop$run_dirs} will be \code{character(0)} even if \code{tools} is non-\code{NULL}).
Default: \code{getOption("tidybreed.db_name", "sim.duckdb")}.}

\item{clean}{Logical. If \code{TRUE} (default), deletes the existing database
file and all timestamped layer-5 run subdirectories inside each tool dir
before recreating. Does \strong{not} delete the tool dirs or the scenario dir.
Silently ignored for in-memory databases.}
}
\value{
A \code{tidybreed_pop} object with all core tables created and
\code{pop$run_dirs} populated (empty \code{character(0)} for in-memory databases).
}
\description{
The single entry point to start a simulation. Creates the DuckDB database,
all core metadata tables, and the per-scenario folder structure (layers 2–4).
Genome tables are added separately by \code{\link[=define_genome]{define_genome()}}.

Reads global options for default argument values so simulations can be
parameterized via \code{options()} or a YAML config without changing the script
body.
\subsection{Folder layout}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{<base_dir>/
  <output_dir>/                   # layer 2 (e.g. "tidybreed_output")
    <scenario_dir>/               # layer 3 (e.g. "scenario_A" or auto timestamp)
      <db_name>                   # the DuckDB file
      blupf90/                    # layer 4 (if "blupf90" in tools)
      plink/                      # layer 4 (if "plink" in tools)
}\if{html}{\out{</div>}}

When \code{db_name = ":memory:"} or an absolute/UNC path, folder creation is
skipped entirely (including layer-4 \code{tools} dirs) and \code{pop$run_dirs} is
\code{character(0)}.
}
}
\examples{
\dontrun{
# In-memory (common in tests and development)
pop <- open_pop(pop_name = "test", db_name = ":memory:") |>
  define_genome(n_loci = 1000, n_chr = 10, chr_len_Mb = 100)

# File-based with folder structure and tool dirs
options(
  tidybreed.output   = "tidybreed_output",
  tidybreed.scenario = "scenario_A",
  tidybreed.tools    = c("blupf90", "plink"),
  tidybreed.db_name  = "sim.duckdb"
)
pop <- open_pop() |>
  define_genome(n_loci = 50000, n_chr = 18, chr_len_Mb = 100)
# creates: tidybreed_output/scenario_A/blupf90/
#          tidybreed_output/scenario_A/plink/
#          tidybreed_output/scenario_A/sim.duckdb

# HPC array job: scenario encoded in option from YAML
options(tidybreed.scenario = paste0("scenario_A_rep_", Sys.getenv("SLURM_ARRAY_TASK_ID")))
pop <- open_pop()
}
}
\seealso{
\code{\link[=define_genome]{define_genome()}} to add genome tables, \code{\link[=restore_pop]{restore_pop()}} to
reconnect to an existing database.
}
