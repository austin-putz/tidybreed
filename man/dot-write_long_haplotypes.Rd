% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haplotype_write_helpers.R
\name{.write_long_haplotypes}
\alias{.write_long_haplotypes}
\title{Insert a long haplotype frame into \code{ind_haplotype} (no UNPIVOT)}
\usage{
.write_long_haplotypes(conn, long_frame)
}
\arguments{
\item{conn}{DuckDB connection.}

\item{long_frame}{data.frame with columns \code{id_ind}, \code{parent_origin},
\code{strand}, \code{line_origin}, \code{locus_id}, \code{allele}.}
}
\value{
Invisibly \code{NULL}.
}
\description{
The single shared write core for \code{add_offspring()} and \code{add_founders()}.
Registers \code{long_frame} and does one \verb{INSERT ... SELECT} that joins
\code{genome_meta} on \code{locus_id} to attach \code{locus_name}. \code{strand} is always \code{1L}
in this ploidy-2 version. RNG-neutral (register + INSERT, no \code{dbWriteTable}).
}
\details{
Write mechanism (Stage-1 Appender vs register+INSERT decision): we use
\code{duckdb_register()} + \verb{INSERT ... SELECT}, not the DuckDB Appender API. It is
RNG-neutral (unlike \code{dbWriteTable}, it advances no R RNG — the founder/offspring
RNG discipline relies on this), zero-copy over the R frame, and already the
fast path the pre-Stage-1 UNPIVOT write used. The Appender API would save only
the per-batch view registration (marginal, and batching already bounds that),
at the cost of a C++ boundary; not worth it here. Revisit if profiling ever
shows registration overhead dominating.
}
\keyword{internal}
