% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recombination_helpers.R
\name{make_gamete}
\alias{make_gamete}
\title{Produce a gamete from a parent's two haplotypes via chromosomal crossovers}
\usage{
make_gamete(hap_matrix, chr_info)
}
\arguments{
\item{hap_matrix}{2 x n_loci integer matrix. Row 1 = haplotype from
parent_origin 1, row 2 = haplotype from parent_origin 2.}

\item{chr_info}{List returned by \code{build_chr_info()}. Contains pre-computed
locus indices, positions, and lengths per chromosome.}
}
\value{
A list with two length-\code{n_loci} integer vectors:
\describe{
\item{\code{allele}}{the gamete alleles (0 or 1).}
\item{\code{homolog}}{which of the parent's two homologs (1 or 2) donated the
allele at each locus. Used by \code{\link[=add_offspring]{add_offspring()}} to inherit \code{line_origin}
from the contributing parental segment.}
}
The random-draw sequence (\code{rpois}, \code{sample}, \code{runif}) is identical to the
allele-only version — \code{homolog} is derived from values already computed, so
seeded output is unchanged.
}
\description{
Simulates chromosomal recombination using the Haldane map function.
Crossover count per chromosome ~ Poisson(chr_len_cM / 100), i.e.
Poisson(genetic length in Morgans) since 1 Morgan = 100 cM. Crossover
positions are uniform in genetic (cM) distance within each chromosome.
}
\keyword{internal}
