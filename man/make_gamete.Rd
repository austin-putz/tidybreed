% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recombination_helpers.R
\name{make_gamete}
\alias{make_gamete}
\title{Produce a gamete from a parent's two haplotypes via chromosomal crossovers}
\usage{
make_gamete(hap_matrix, chr_info, base_seed, sid, store_crossovers = FALSE)
}
\arguments{
\item{hap_matrix}{2 x n_loci integer matrix. Row 1 = haplotype from
parent_origin 1, row 2 = parent_origin 2.}

\item{chr_info}{List from \code{build_chr_info()} — pre-computed locus indices,
positions, and lengths per chromosome.}

\item{base_seed}{Integer base seed for the \code{add_offspring()} call.}

\item{sid}{Integer stream id (see \code{\link[=.gamete_stream_id]{.gamete_stream_id()}}, kind = 1 autosome).}

\item{store_crossovers}{Logical; emit the crossover buffer when \code{TRUE}.}
}
\value{
List: \code{allele} and \code{homolog} (length-\code{n_loci} integer vectors, the
gamete alleles and the donating homolog per locus), plus \code{xover_chr_idx}
(the \code{chr_info} position of each crossover event) and \code{xover_pos_cM} (its
cM position) — both \code{length(0)} unless \code{store_crossovers}.
}
\description{
Simulates chromosomal recombination using the Haldane map function, drawing
from this gamete's own dqrng stream (seeded here from \code{base_seed} + \code{sid}).
Crossover count per chromosome ~ Poisson(chr_len_cM / 100), i.e.
Poisson(genetic length in Morgans); positions uniform in genetic (cM)
distance. All chromosomes of the gamete are drawn sequentially from the one
seeded stream in ascending-chromosome order.
}
\keyword{internal}
