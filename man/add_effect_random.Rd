% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_effect_random.R
\name{add_effect_random}
\alias{add_effect_random}
\title{Add a random group effect to a trait's phenotype model}
\usage{
add_effect_random(
  pop,
  trait_name,
  effect_name,
  source_column,
  variance = NULL,
  distribution = c("normal", "gamma", "uniform"),
  source_table = "ind_meta",
  overwrite = FALSE
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object.}

\item{trait_name}{Character. Name of an existing trait.}

\item{effect_name}{Character. Unique label for this effect within the trait.}

\item{source_column}{Character. Column in \code{source_table} whose distinct
values define the groups (e.g. \code{"herd_id"}, \code{"litter"}).}

\item{variance}{Numeric. Variance of the random effect distribution.
Optional if a variance has already been stored via \code{\link[=add_effect_cov_matrix]{add_effect_cov_matrix()}}
for this \code{effect_name} and \code{trait_name}. Required otherwise.}

\item{distribution}{Character. Sampling distribution: \code{"normal"} (default),
\code{"gamma"}, or \code{"uniform"}.}

\item{source_table}{Character. Database table that contains \code{source_column}.
Defaults to \code{"ind_meta"}. Must contain an \code{id_ind} column.}

\item{overwrite}{Logical. Replace an existing effect with the same name.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Inserts a row into \code{trait_effects} for a random effect. One value is drawn
per distinct level of \code{source_column}; all individuals sharing that level
receive the same shift. Drawn values are stored in \code{trait_random_effects}
so they are reproducible across repeated calls to \code{\link[=add_phenotype]{add_phenotype()}} without
requiring a fixed \code{seed}.

To correlate this effect across multiple traits (e.g. the same herd affects
both ADG and BW), call \code{\link[=add_effect_cov_matrix]{add_effect_cov_matrix()}} with the appropriate
\code{effect_name} — either before or after this call.
}
\examples{
\dontrun{
pop <- pop |>
  add_effect_random("ADG", "herd",
    source_column = "herd_id",
    variance = 150)
}
}
\seealso{
\code{\link[=add_effect_cov_matrix]{add_effect_cov_matrix()}}, \code{\link[=add_effect_fixed_class]{add_effect_fixed_class()}},
\code{\link[=add_effect_fixed_cov]{add_effect_fixed_cov()}}, \code{\link[=add_phenotype]{add_phenotype()}}
}
