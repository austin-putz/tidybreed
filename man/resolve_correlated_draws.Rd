% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlated_draws.R
\name{resolve_correlated_draws}
\alias{resolve_correlated_draws}
\title{Draw coordinates of a Gaussian block conditional on observed coordinates}
\usage{
resolve_correlated_draws(
  covariance,
  sample_coordinates,
  entity_keys,
  observed = NULL,
  tolerance = NULL
)
}
\arguments{
\item{covariance}{Numeric matrix with dimnames: one stratum of a block, as
returned by \code{\link[=find_covariance_blocks]{find_covariance_blocks()}}.}

\item{sample_coordinates}{Character vector of the coordinates to draw, a
subset of the block's names with no duplicates. May be empty.}

\item{entity_keys}{Opaque entity identifiers: a vector or list (one entity
per element) or a data frame (one entity per row). Only the count is used,
and it fixes \code{n}. Rows of the result follow its order.}

\item{observed}{\code{NULL}, or a numeric matrix / data frame with \code{n} rows whose
column names are block coordinates not in \code{sample_coordinates}. \code{NA}
means "not observed for this entity"; every non-\code{NA} value must be
finite. Stored and caller-fixed values both go here.}

\item{tolerance}{Relative tolerance; see the numerical contract. \code{NULL}
uses the default.}
}
\value{
A numeric \code{n} x \code{length(sample_coordinates)} matrix with the sample
coordinates as column names, rows in \code{entity_keys} order.
}
\description{
For \code{n} entities sharing one covariance matrix \code{covariance} over a block of
coordinates, draws the \code{sample_coordinates} of every entity from the
multivariate normal conditional on that entity's observed coordinates. An
entity's observed set is whatever is non-\code{NA} in its row of \code{observed};
entities with different observed sets are grouped by pattern and the
conditional mean coefficients and covariance are computed once per pattern.
Coordinates that are neither observed nor sampled are latent and are never
realized.

The function is pure apart from RNG: it never reads or writes a database and
knows nothing about individuals, strata or tables.
}
\section{Numerical contract}{

\itemize{
\item \code{tolerance} is \emph{relative}. The absolute tolerance is
\code{tolerance * lambda_max(covariance)}, so the same relative perturbation is
treated the same way at variance \code{1e-6} and \code{1e6}. The default is
\code{nrow(covariance) * sqrt(.Machine$double.eps)}.
\item \code{covariance} must be square, finite, symmetric within tolerance, with
identical unique row and column names, and positive semi-definite within
tolerance. Perfect correlation and zero-variance coordinates are valid.
\item The observed block \code{R_oo} is inverted by Cholesky when it is positive
definite and by an eigen pseudoinverse when it is only semi-definite. In
the singular case every observed vector must lie in the support of its
Gaussian: the component of \code{e_o} outside the range of \code{R_oo} must be
within \verb{tolerance * max(||e_o||, sqrt(lambda_max))} of zero. A vector
outside the support (a non-zero value for a zero-variance coordinate, or
two perfectly correlated coordinates that disagree) has probability zero
and defines no conditional distribution, so it is an error rather than a
pseudoinverse guess.
\item The conditional covariance is symmetrized; eigenvalues in
\verb{[-tolerance * lambda_max, 0)} are projected to zero and anything more
negative is an error. It is factored by Cholesky when positive definite
(platform-deterministic) and by \verb{V sqrt(D)} otherwise. A coordinate with
zero conditional variance is returned at its conditional mean exactly.
\item \strong{RNG.} Every check runs before the first random number is drawn, so a
rejected call leaves \code{.Random.seed} untouched. A successful call consumes
exactly \code{n * length(sample_coordinates)} standard normals from
\code{\link[stats:rnorm]{stats::rnorm()}}, in entity order and \code{sample_coordinates} order within
an entity, whatever the observed patterns and even when some conditional
variances are zero. Zero entities or zero sample coordinates consume
nothing.
}
}

\keyword{internal}
