% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_allele_freq.R
\name{extract_allele_freq}
\alias{extract_allele_freq}
\title{Extract per-locus allele frequencies from a filtered table}
\usage{
extract_allele_freq(tbl)
}
\arguments{
\item{tbl}{A \code{tidybreed_table} from \code{\link[=get_table]{get_table()}}, optionally filtered. One
of the three shapes above. Columns removed with \code{\link[dplyr:select]{dplyr::select()}} are
checked before any SQL runs.}
}
\value{
A tibble with exactly one row per \code{genome_meta} locus in ascending
\code{locus_id} order: \code{locus_id} (integer), \code{locus_name} (character),
\code{allele_freq} (double). \code{allele_freq} is \code{NA} at a locus the selection has
no copies for; it is never \code{0} in that case, and an observed frequency of
exactly \code{0} or \code{1} is a real value and is kept. Errors if no locus has a
copy at all. Never warns and never writes.
}
\description{
Computes the frequency of allele 1 at every locus in \code{genome_meta} from the
allele copies a filtered \code{tidybreed_table} selects. The table's identity
says \emph{what kind of thing} is being selected; the user's \code{\link[dplyr:filter]{dplyr::filter()}}
says \emph{which ones}:\tabular{lll}{
   Piped table \tab Meaning \tab Copies counted \cr
   \code{founder_haplotypes} \tab the founder pool \tab the filtered pool rows \cr
   \code{ind_haplotype} \tab these allele copies \tab the filtered haplotype rows \cr
   any other table with \code{id_ind} \tab these individuals \tab every \code{ind_haplotype} row of the \strong{distinct} selected \code{id_ind} \cr
}


The frequency is computed in one SQL statement with the filter rendered as
a subquery; nothing but the per-locus result is collected into R. For an
\code{id_ind} table the frequency depends only on which individuals are selected,
never on how many rows each has — \code{ind_phenotype} with five records per
animal gives the same answer as \code{ind_meta} for the same animals.

This is the single place tidybreed turns a population selection into \code{p}.
\code{\link[=define_additive_effects]{define_additive_effects()}} and \code{\link[=define_genome_effects]{define_genome_effects()}} both call it for
their \code{base_tbl}, so a base selection means the same population in either.
Users call it to obtain \code{p} for \code{\link[=ad_terms]{ad_terms()}}.
}
\section{Two notions of line}{

\code{ind_meta.line_name} is a pedigree label — an F1 is whatever it was called;
\code{ind_haplotype.line_origin} is the founding line each allele copy traces to,
carried through every \code{\link[=add_offspring]{add_offspring()}} call. They coincide for purebreds and
diverge for crosses. \code{get_table(pop, "ind_meta") |> filter(line_name == "F1")} counts every copy those animals carry, whichever line it came from;
\code{get_table(pop, "ind_haplotype") |> filter(line_origin == "Duroc")} counts
Duroc copies wherever they sit, at any cross depth.
}

\examples{
\dontrun{
# Founder pool of one line
p <- pop |> get_table("founder_haplotypes") |>
  dplyr::filter(line_name == "Duroc") |> extract_allele_freq()

# Generation-0 animals
p <- pop |> get_table("ind_meta") |> dplyr::filter(gen == 0L) |>
  extract_allele_freq()

# Duroc copies wherever they sit, including inside crossbreds
p <- pop |> get_table("ind_haplotype") |>
  dplyr::filter(line_origin == "Duroc") |> extract_allele_freq()

# Feed p to ad_terms()
loci <- c("Locus_10", "Locus_44")
tt <- ad_terms(loci, a = c(0.4, 0.1), d = c(0.2, 0.05),
               p = p$allele_freq[match(loci, p$locus_name)],
               coding = "cockerham")
}
}
\seealso{
\code{\link[=define_additive_effects]{define_additive_effects()}}, \code{\link[=define_genome_effects]{define_genome_effects()}}, \code{\link[=ad_terms]{ad_terms()}}.
}
