% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_phenotype_stages.R
\name{.ap_plan}
\alias{.ap_plan}
\title{Stage 1: plan every record of an \code{add_phenotype()} call}
\usage{
.ap_plan(tbl, phenos, user_values = NULL)
}
\arguments{
\item{tbl}{The \code{tidybreed_table} passed to \code{add_phenotype()}.}

\item{phenos}{Character vector of validated phenotype names.}

\item{user_values}{The \code{user_values} argument, or \code{NULL}.}
}
\value{
\code{NULL} when no individual matched (a warning is issued), otherwise
a list with \code{pop} (after \code{add_tbv()}), \code{phenos} (in evaluation order),
\code{pheno_meta} (rows in that order) and \code{entries}: one list per phenotype,
in the same order, each with
\describe{
\item{\code{phenotype_name}}{}
\item{\code{path}}{\code{"model"}, \code{"derived_formula"} or \code{"user_values"}.}
\item{\code{id_ind}, \code{pheno_number}}{The planned records, \code{id_ind}-ordered.
\code{pheno_number} is the value Stage 3 writes.}
\item{\code{tbv}}{Numeric per record (\code{"model"} path only).}
\item{\code{fixed}}{Fixed-effect contribution per record (\code{"model"} path;
zeros for \code{"derived_formula"}, which has no model terms).}
\item{\code{random}}{Random-effect terms: a list of
\code{list(effect_name, distribution, level)} where \code{level} is the
character level of the grouping column per record (\code{NA} = none).}
\item{\code{condition_table}, \code{condition_column}, \code{condition_value}}{The
residual stratum lookup: \code{NULL} when the phenotype's residual block
is unconditional or absent, otherwise the raw condition value per
record as character (\code{NA} = \code{NULL} in the table).}
\item{\code{user_values}}{Numeric per record (\code{"user_values"} path).}
\item{\code{formula}}{The derived formula string (\code{"derived_formula"} path).}
}
and the covariance blocks Stage 2 draws through, from
\code{\link[=find_covariance_blocks]{find_covariance_blocks()}}: \code{residual_blocks} (the residual blocks
touching the call's phenotypes), \code{named_targets} (per random effect,
the model-path phenotypes whose planned records carry it; see
\code{.ap_named_effect_targets()}) and \code{named_blocks} (per effect, the
blocks touching those phenotypes).
}
\description{
Stage 1: plan every record of an \code{add_phenotype()} call
}
\keyword{internal}
