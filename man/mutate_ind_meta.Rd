% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_ind_meta.R
\name{mutate_ind_meta}
\alias{mutate_ind_meta}
\title{Add or modify columns in individual metadata table}
\usage{
mutate_ind_meta(pop, ...)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object}

\item{...}{Named arguments specifying column names and values.
Each argument should be \code{field_name = value}, where value is either:
\itemize{
\item A scalar (applied to all individuals)
\item A vector of length equal to number of individuals
}}
}
\value{
The \code{tidybreed_pop} object (invisibly), modified in place
}
\description{
Adds new columns to the \code{ind_meta} table or updates existing custom columns.
Values can be scalar (applied to all individuals) or vectors (one per individual).
The function automatically infers DuckDB column types from R data types.

Reserved columns (\code{ind_id}, \code{parent_1}, \code{parent_2}, \code{population}, \code{sex}) cannot be modified.
}
\details{
\strong{Type Inference:}
\itemize{
\item \code{logical} → BOOLEAN
\item \code{integer} → INTEGER
\item \code{numeric} → DOUBLE
\item \code{Date} → DATE
\item \code{POSIXct}/\code{POSIXlt} → TIMESTAMP
\item \code{character} → VARCHAR
}

\strong{Field Naming Rules:}
\itemize{
\item Must start with a letter
\item Can contain letters, numbers, and underscores
\item Cannot be SQL reserved words
\item Cannot be reserved columns (ind_id, parent_1, parent_2, population, sex)
}
}
\examples{
\dontrun{
# Initialize and add founders
pop <- initialize_genome(pop_name = "A", n_loci = 1000, n_chr = 10) \%>\%
  add_founders(n_males = 10, n_females = 100, population = "A")

# Add scalar fields (same value for all individuals)
pop <- pop \%>\%
  mutate_ind_meta(
    gen = 0,
    farm = "A",
    date_birth = Sys.Date()
  )

# Add vector field (different value per individual)
n_ind <- 110
pop <- pop \%>\%
  mutate_ind_meta(
    weight_kg = rnorm(n_ind, mean = 80, sd = 10),
    is_selected = sample(c(TRUE, FALSE), n_ind, replace = TRUE)
  )

# Update existing field
pop <- pop \%>\%
  mutate_ind_meta(gen = 1)

# View updated metadata
get_table(pop, "ind_meta") \%>\% collect()
}
}
