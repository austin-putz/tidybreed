% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_pop.R
\name{summary.tidybreed_pop}
\alias{summary.tidybreed_pop}
\title{Summarize a tidybreed population}
\usage{
\method{summary}{tidybreed_pop}(object, tables = NULL, max_values = 12L, ...)
}
\arguments{
\item{object}{A \code{tidybreed_pop} object.}

\item{tables}{Character vector of table names to include. \code{NULL} (default)
includes all tables registered in \code{object$tables}.}

\item{max_values}{Integer. Columns with at most this many distinct values
receive a frequency table display; default \code{12L}.}

\item{...}{Additional arguments (not used; required by the S3 generic).}
}
\value{
A \code{tidybreed_summary} object (a named list).
}
\description{
Produces a detailed summary of all tables in a \code{tidybreed_pop} object.
For each table, shows row count, column count, and per-column statistics:
frequency tables for low-cardinality categorical columns, 5-number
summaries for numeric columns, and date ranges for date/timestamp columns.
Wide genome tables (\code{genome_haplotype}, \code{genome_genotype}) only summarize
their non-locus metadata columns to avoid querying 50,000-column tables.
}
\examples{
\dontrun{
pop <- initialize_genome("MySim", n_loci = 1000, n_chr = 5, chr_len_Mb = 100,
                         n_haplotypes = 50)
pop <- add_founders(pop, n_males = 50, n_females = 50, line_name = "A", gen = 0L)
summary(pop)
summary(pop, tables = c("ind_meta", "genome_meta"))
summary(pop, max_values = 5L)
}
}
\seealso{
\code{\link[=print.tidybreed_pop]{print.tidybreed_pop()}}
}
