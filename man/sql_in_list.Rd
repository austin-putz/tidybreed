% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql_utils.R
\name{sql_in_list}
\alias{sql_in_list}
\title{Quote and escape a character vector for a SQL \code{IN (...)} list}
\usage{
sql_in_list(values, what = "value")
}
\arguments{
\item{values}{Character vector to escape and format for a SQL \code{IN (...)}
list. Must be non-empty and free of \code{NA} (a bare \code{NA} would otherwise
silently become the literal string \code{'NA'} in SQL).}

\item{what}{Human-readable role label used in error messages
(e.g. \code{"locus name"}, \code{"individual ID"}).}
}
\value{
A single character string: comma-separated, single-quoted,
escaped values.
}
\description{
Escapes embedded single quotes (quote-doubling, matching
\code{format_sql_value()}'s VARCHAR branch) and returns the comma-joined,
quoted fragment only — callers still write \code{paste0("... IN (", x, ")")}
around the result.
}
\keyword{internal}
