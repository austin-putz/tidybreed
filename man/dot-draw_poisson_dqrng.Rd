% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recombination_helpers.R
\name{.draw_poisson_dqrng}
\alias{.draw_poisson_dqrng}
\title{Draw a Poisson count from the current dqrng stream (log-accumulation inversion)}
\usage{
.draw_poisson_dqrng(lambda)
}
\arguments{
\item{lambda}{Non-negative Poisson rate. \code{lambda <= 0} returns \code{0L} and draws
nothing. Errors above a documented ceiling (30 Morgans / 3000 cM) rather
than spinning \code{O(lambda)} uniforms.}
}
\value{
Integer crossover count.
}
\description{
Knuth's multiplicative inversion in log space: accumulate \code{log(u)} and count
steps until the sum drops to \code{-lambda}. Consumes ONLY dqrng uniforms (no base
\code{rpois}, no \code{std::poisson_distribution}), so the R reference and the future
C++ kernel match by construction from the shared uniform stream. Log form
(not the raw product) avoids underflow when \code{exp(-lambda)} is denormal.
}
\keyword{internal}
