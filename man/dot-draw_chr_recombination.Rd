% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recombination_helpers.R
\name{.draw_chr_recombination}
\alias{.draw_chr_recombination}
\title{Draw one chromosome's recombination pattern from the current dqrng stream}
\usage{
.draw_chr_recombination(chr_pos, chr_len, store_crossovers = FALSE)
}
\arguments{
\item{chr_pos}{Numeric locus genetic positions (cM) for this chromosome.}

\item{chr_len}{Numeric chromosome genetic length (cM) = \code{max(chr_pos)}.}

\item{store_crossovers}{Logical; when \code{TRUE} also return the drawn crossover
positions (cM) for \code{ind_crossover}.}
}
\value{
List with \code{hap_idx_vec} (length \code{length(chr_pos)}, values in \verb{\{1,2\}}
— which homolog donates each locus) and \code{xover_pos_cM} (sorted crossover
positions in cM, or \code{numeric(0)}).
}
\description{
The per-chromosome core shared by autosomes (\code{\link[=make_gamete]{make_gamete()}}) and recombining
special chromosomes (the \code{add_offspring()} special path). Assumes the gamete's
stream is ALREADY seeded — draws sequentially in the fixed canonical order
(count, then start homolog, then sorted positions) so R↔C++ parity holds.
Purely a homolog-index computation: it never touches allele values, so the
caller gathers alleles/\code{line_origin} from its own matrices via \code{hap_idx_vec}.
}
\keyword{internal}
