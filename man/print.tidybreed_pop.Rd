% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidybreed_pop.R
\name{print.tidybreed_pop}
\alias{print.tidybreed_pop}
\title{Print method for tidybreed_pop}
\usage{
\method{print}{tidybreed_pop}(x, ...)
}
\arguments{
\item{x}{A tidybreed_pop object}

\item{...}{Additional arguments (not used)}
}
\value{
\code{x}, invisibly.
}
\description{
Prints a grouped, human-readable summary of the population: a header rule
with the population name, the database location and connection status, then
one section each for the \strong{genome} (loci, chromosomes, physical/genetic
length, founder haplotype pool), the \strong{model} (genetic-component traits,
observed phenotypes, selection indices, QTL), the \strong{individuals} (total,
broken down by sex and by line), and the \strong{records} written so far
(phenotypes, TBVs, EBVs, index values).

Sections are shown only when their underlying data exists, so a freshly
opened population collapses to just the header, the database line, and a
pointer to \code{\link[=schema]{schema()}} / \code{\link[=describe_table]{describe_table()}}. Counts are queried live from the
DuckDB connection; a closed connection prints as \verb{[disconnected]} without
running any queries.
}
