% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive_replicate.R
\name{archive_replicate}
\alias{archive_replicate}
\title{Archive a completed replicate and reset the working database}
\usage{
archive_replicate(
  pop,
  replicate = getOption("tidybreed.replicate"),
  archive_path = NULL,
  store_and_reset = c("ind_meta", "ind_phenotype", "ind_tbv", "ind_ebv", "ind_index",
    "ind_true_index"),
  store_once = c("genome_meta", "genome_effects", "trait_meta", "phenotype_effects",
    "trait_var_comp", "phenotype_meta", "phenotype_components", "phenotype_var_comp",
    "index_meta"),
  reset_only = c("ind_haplotype", "ind_genotype", "ind_crossover")
)
}
\arguments{
\item{pop}{A \code{tidybreed_pop} object with an open DuckDB connection.}

\item{replicate}{Integer scalar. Replicate number stamped on every archived
row in \code{store_and_reset} tables. Defaults to
\code{getOption("tidybreed.replicate")} (starts at \code{1L}). Always increments the
option by 1 after a successful archive so the next loop iteration uses the
next number automatically.}

\item{archive_path}{Character or \code{NULL}. Full path to the archive DuckDB
file. When \code{NULL} (default) the path is resolved from global options — see
Details. Passing \code{NULL} and leaving both archive options unset means only
the reset phases run (no archive is written).}

\item{store_and_reset}{Character vector. Tables to archive with a \code{replicate}
column and then DELETE from the working DB after archiving succeeds.}

\item{store_once}{Character vector. Tables copied to the archive on the first
call only (detected by whether the table already exists in the archive
schema). Working-DB rows are left intact. Use for static configuration that
does not change between replicates (trait definitions, genome structure,
variance components, index weights).}

\item{reset_only}{Character vector. Tables whose rows are deleted from the
working DB without being archived. Intended for large genomic tables
(\code{ind_haplotype}, \code{ind_genotype}, \code{ind_crossover}) that are unnecessary for
most downstream analyses.}
}
\value{
\code{pop} invisibly (pipeline-compatible).
}
\description{
Stamps per-replicate tables with a \code{replicate} column and appends them to a
separate archive DuckDB file, copies static metadata tables to the archive on
the \strong{first call only} (no \code{replicate} column), then deletes the working-DB
rows so the next replicate starts clean. This is the only function that
writes to an archive DB; the working DB stays lean throughout the loop.
}
\details{
\strong{Archive path resolution} — first non-\code{NULL} result wins:
\enumerate{
\item Explicit \code{archive_path} argument (full file path)
\item \code{file.path(tidybreed.archive_path, tidybreed.db_name_archive)} when both
options are set
\item \code{file.path(dirname(pop$db_path), tidybreed.db_name_archive)} when only
\code{tidybreed.db_name_archive} is set (archive lands next to the working DB)
\item \code{tidybreed.db_name_archive = NULL} → \code{archive_path} resolves to \code{NULL} →
skip all archive writes; only the reset phases run
}

\strong{Individual-ID invariant}: \code{id_ind} is \strong{not} globally unique across
replicates, even in a single-process sequential loop. \code{archive_replicate()}
deletes \code{ind_meta} (a \code{store_and_reset} table) after archiving, so
\code{\link[=add_founders]{add_founders()}} and \code{\link[=add_offspring]{add_offspring()}} see an empty table on the next
replicate and restart numbering from \verb{\{line_name\}_1}. Every replicate (and
every HPC parallel job, which independently restarts numbering too) reuses
the same \code{id_ind} values — always use \verb{(replicate, id_ind)} as the
composite key in archive queries.

\strong{HPC note}: concurrent writes from multiple jobs to one shared archive file
are \strong{not supported}. Use per-job archive files
(\code{scenario_rep_007.duckdb}) and merge in a post-processing step.
}
\section{Usage patterns}{


\strong{In-process loop}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(tidybreed.replicate = 1L)
options(tidybreed.db_name_archive = "scenario_1_all_reps.duckdb")

pop <- open_pop(...) |> define_genome(...) |>
  define_trait(...) |> define_phenotype(...)

for (i in seq_len(100)) \{
  pop <- pop |>
    get_table("founder_haplotypes") |>
    add_founders(n_males = 10, n_females = 90, line_name = "A") |>
    get_table("ind_meta") |>
    add_phenotype("ADG") |>
    archive_replicate()
\}

# post-hoc analysis. When only 'tidybreed.db_name_archive' is set (as
# above), the archive file is written next to the working DB, which may
# itself be nested under open_pop()'s layer-2/3 folders (see ?open_pop) —
# so resolve the path from the pop object rather than assuming a bare
# filename in the current working directory.
archive_path <- file.path(dirname(pop$db_path), "scenario_1_all_reps.duckdb")
archive <- restore_pop(archive_path)
get_table(archive, "ind_phenotype") |>
  dplyr::filter(replicate == 5L) |>
  dplyr::collect()
}\if{html}{\out{</div>}}

\strong{HPC / SLURM array} (one job per replicate):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{rep_id <- as.integer(Sys.getenv("SLURM_ARRAY_TASK_ID"))
options(tidybreed.replicate = rep_id)
options(tidybreed.db_name_archive = paste0("scenario_1_rep_", rep_id, ".duckdb"))
pop <- open_pop(...) |> ... |> archive_replicate()
}\if{html}{\out{</div>}}
}

