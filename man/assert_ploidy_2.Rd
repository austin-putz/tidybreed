% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ploidy_helpers.R
\name{assert_ploidy_2}
\alias{assert_ploidy_2}
\title{Assert that a set of individuals all have ploidy = 2}
\usage{
assert_ploidy_2(pop, ids = NULL)
}
\arguments{
\item{pop}{A \code{tidybreed_pop}.}

\item{ids}{Optional character vector of \code{id_ind} to check; \code{NULL} (default)
checks every individual in \code{ind_meta}.}
}
\value{
Invisible \code{NULL} on success; errors otherwise.
}
\description{
Ploidy-aware dosage and genotype extraction for real polyploidy (ploidy > 2)
is not yet supported. Sex-linked and organelle \code{chr_meta} rules
(\code{copy_mode_M}/\code{copy_mode_F}) are fine here — \code{SUM(allele)} over however
many \code{ind_haplotype} rows exist per locus already produces correct dosage
regardless of row count, so this guard targets actual organism ploidy, not
\code{chr_meta} shape.
}
\keyword{internal}
