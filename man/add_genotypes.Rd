% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_genotypes.R
\name{add_genotypes}
\alias{add_genotypes}
\title{Mark animals as genotyped on a SNP chip}
\usage{
add_genotypes(tbl, chip_name, col_name = paste0("has_", chip_name))
}
\arguments{
\item{tbl}{A \code{tidybreed_table} object from \code{\link[=get_table]{get_table()}} (optionally piped
through \code{\link[=filter]{filter()}}). The table must contain an \code{id_ind} column when a
filter is applied.}

\item{chip_name}{Character. Name of an existing SNP chip (must have an
\verb{is_<chip_name>} column in \code{genome_meta}, created by \code{\link[=define_chip]{define_chip()}}).}

\item{col_name}{Character. Name of the BOOLEAN column to write in
\code{ind_meta}. Default: \code{paste0("has_", chip_name)}.}
}
\value{
The modified \code{tidybreed_pop} (invisibly).
}
\description{
Records which individuals have been genotyped on a named SNP chip by
writing or updating a BOOLEAN column \verb{has_<chip_name>} in \code{ind_meta}.
Pipe a \code{tidybreed_table} (from \code{\link[=get_table]{get_table()}} and optionally \code{\link[=filter]{filter()}}) as
the first argument to restrict which animals are marked.

The operation is \strong{additive}: animals already marked \code{TRUE} remain \code{TRUE}.
Only new animals are flipped. This mirrors real life — once an animal is
genotyped it stays genotyped.
}
\examples{
\dontrun{
# Genotype all females in generation 1 on the 50k chip
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(sex == "F", gen == 1L) |>
  add_genotypes("50k")

# Also genotype all generation 2 animals (additive — gen 1 females stay TRUE)
pop <- pop |>
  get_table("ind_meta") |>
  dplyr::filter(gen == 2L) |>
  add_genotypes("50k")
}
}
